# `identidad` — Contexto para agentes

Usuarios, grupos, permisos y accesos: **quien puede hacer que, en que municipalidad**. Es el
**dueño de la autorizacion**, y no autentica a nadie.

El quinto repositorio de **Kamayuk**, el producto multi-municipal que reimplementa el sistema
documentado en el manual de usuario del SGTM de la Municipalidad Provincial de Sullana. Los otros
cinco son [`infrastructure`](https://github.com/hneyra/infrastructure) —el suelo y las barreras—,
[`rentas`](https://github.com/hneyra/rentas), [`catastro`](https://github.com/hneyra/catastro),
[`normativa`](https://github.com/hneyra/normativa) y [`caja`](https://github.com/hneyra/caja). El
reparto lo decide
[ADR-0029](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/adr/ADR-0029-cuatro-sistemas-separados.md);
que este sistema exista lo decide
[ADR-0039](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/adr/ADR-0039-la-identidad-es-un-sistema.md),
al contestar D-19.

**Este es el sistema mas nuevo, y eso cambia como se lee todo lo de abajo**: casi nada de lo que
los otros cinco aprendieron por su cuenta se ha vuelto a descubrir aqui — se copio con su
comentario. Cuando algo diga «medido», lo que hay detras es la medicion del hermano que lo
descubrio, con su referencia; lo medido **aqui** va en la tabla del final, y esa **nace vacia**.

## Que hay hoy, medido y no supuesto

| Pieza | Estado |
|---|---|
| `infrastructure/` — el descriptor de despliegue | **Existe.** `yarn verificar` en verde —lint, tipos y **21 pruebas**—, sin Pulumi, sin token y sin cluster |
| `despliegue/compose.yaml` | **Existe**, con tres servicios encadenados contra la plataforma. **NO se ha levantado**: la maquina donde se escribio no tiene demonio de Docker, y decir que funciona seria deducirlo de que el YAML analiza |
| `.github/workflows/` | **Existe desde el primer commit**, con **cuatro** flujos: `backend.yml`, `infraestructura.yml`, `registro.yml` y `publicar-imagenes.yml`. **Sin `frontend.yml` ni `documentacion.yml`**, y es una afirmacion: no hay pantalla ni corpus |
| `docs/00-gobierno/` — la guarda del registro y su autoprueba | **Existen y corren en verde**: 9 muestras, 3 rutas de codigo de produccion, todas con muestra que las ejerza |
| `backend/` — seis modulos | Lo aporta el otro carril del issue #1. Su estado real lo dice **su** fila del registro, no esta tabla |
| `backend/kamayuk-identidad-nucleo` — el contexto acotado | **Ya no esta vacio** (etapa 2, [#2](https://github.com/hneyra/identidad/issues/2)). Las **once escrituras** de administracion —altas, bajas, reactivaciones, vigencias, la afiliacion y las dos matrices de permisos—, sus dos repositorios, los **cuatro controladores** bajo `/identidad/api/v1/seguridad`, el buzon de salida y la implantacion. Copiado de `rentas@33f329a2` (`kamayuk-rentas-seguridad`): `git mv` no cruza repositorios, asi que **la historia de esas clases se queda en el `git log` de `rentas`** |
| `identidad_evento` — el buzon de salida | **Existe** (`V2`). Siete tipos, el cuerpo con la fila **entera** tal como quedo, y la emision **dentro de la misma transaccion** que la escritura. **Sin `estado`**, e inmutable: hay cuatro consumidores y una sola columna no puede decir «entregado a `caja` y no a `rentas`» |
| `identidad_evento_acuse` — el acuse, por consumidor | **Existe** (`V3`, etapa 3). Clave `(municipalidad_id, consumidor, evento_id)` —el consumidor va DENTRO de la clave, y esa es toda la tabla—, `CHECK` de los **cuatro** que consumen (`identidad` no se consume a si mismo), RLS `ENABLE`+`FORCE`, e `INSERT, SELECT` y nada mas |
| El buzon **servido** | **Existe** (etapa 3): `GET /identidad/api/v1/eventos/pendientes` y `POST /identidad/api/v1/eventos/acuses`, con la opcion `eventos` del catalogo. **Quien pregunta sale del `azp` del token de servicio** —`kamayuk-<sistema>-servicio-<ubigeo>`— y nunca de un parametro. **Lo consumen los cuatro desde la etapa 4** ([#4](https://github.com/hneyra/identidad/issues/4)): cada uno con su ingestor, que declara lo que lee en `docs/50-api/contratos-que-consume/identidad.json` y se comprueba **aqui**, en `ContratoCon<Consumidor>Test`. Los cuatro contratos piden lo mismo —las dos operaciones, los siete campos del evento, `quedan`, `limite` y el cuerpo del acuse— y los cuatro cumplen. **Y desde la etapa 4 los cuatro pueden leerlo**: la implantacion da de alta sus cuatro cuentas de servicio y las afilia al grupo «Consumidores del buzon» |
| El grupo «Consumidores del buzon» y sus cuatro miembros | **Existe con sus cuatro cuentas dentro** desde la etapa 4 ([#10](https://github.com/hneyra/identidad/pull/10)). La implantacion da de alta `service-account-kamayuk-<sistema>-servicio-<ubigeo>` —los **cuatro** de `Consumidor`, sin `identidad`, que no se consume a si mismo— y las afilia, **emitiendo sus altas y sus afiliaciones por el buzon** como cualquier otra escritura. Nacia vacio hasta la etapa 3 «porque a quien se afilia lo decide quien despliegue»; **medido con las cinco aplicaciones levantadas, no lo decidia nadie**: el emisor crea el cliente confidencial de cada satelite, asi que el consumidor consigue su token, y aqui recibia **403 «la cuenta … no esta dada de alta en este sistema»**. Lo que sigue siendo del despliegue es que esas cuentas existan en el **emisor** (`reconciliar-identidades.sh servicios`) |
| `docs/10-negocio/catalogo-de-accesos/` | **Existe**, con los **cinco** catalogos —`rentas` **130** desde la etapa 4 (eran 134: su AC-4 retiro `usuarios`, `grupos`, `miembros` y `permisos`, y **conservo** `modulos` y `accesos`, que son lecturas de su copia local), `catastro` 16, `identidad` **7** —las seis del manual mas `eventos`, que la etapa 3 anade y que no es una pantalla—, `caja` 3, `normativa` 1: **157 opciones**—. El de `rentas` se **deriva** de su clon hermano con `derivar-catalogo-de-rentas.mjs`; los otros cuatro estan transcritos de su `CatalogoDelSistema`. Lo que impide que las cinco copias se separen de sus originales **no vive aqui**: es la guarda cruzada de `infrastructure` (AC-4) |
| Las once escrituras de `rentas` | **Ya no estan alli** (etapa 4, `rentas@d4c3fb5`): con su consumidor construido, `rentas` retiro sus once casos de uso, sus dos repositorios, tres controladores y las cuatro opciones de `SEGURIDAD` que los servian. **Desde la etapa 4 este es el unico sitio donde se administra.** Entre la 2 y la 4 hubo dos, y fue deliberado (AC-7) |
| `docs/30-arquitectura/adr/` | **El indice, y ningun ADR propio.** Es correcto: la decision que crea este sistema es ADR-0039 y vive en `infrastructure`; copiarla aqui daria dos documentos sobre lo mismo |
| Las imagenes `ghcr.io/hneyra/kamayuk-identidad{,-migrador}` | **NO existen.** El descriptor las nombra igual, y es correcto en esta etapa |
| Que `infrastructure` lo componga | **Si, medido el 2026-09-10.** `infra/descriptor/sistemas.ts:22,33` lo importa y lo registra en `SISTEMAS`, `infra/config.ts:373` lo lleva en `SISTEMAS_CON_IMAGEN`, y `infra/verificaciones/compose-de-los-sistemas.ts:124` ya tiene la excepcion `identidad → identidad-sistema` fijada en las dos direcciones. **El «rojo ruidoso» que anuncia `despliegue/compose.yaml:48-56` esta cerrado**, y esta fila decia lo contrario hasta hoy |

## Lo que este repositorio NO hace

- **NO autentica.** Eso es Keycloak, y ya era uno solo para los cuatro (ADR-0005, ADR-0030 §3).
  ADR-0039 §«No toca» lo dice con todas las letras. Aqui **no se guarda ninguna contrasena**: lo
  que une la fila de `usuario` con la identidad del token es la cuenta, y la cuenta la crea
  `reconciliar-identidades.sh` en el emisor (ADR-0012).
- **NO decide que opciones tiene cada sistema.** El catalogo de opciones sigue siendo de su dueno
  —`rentas` deriva sus **130** de `docs/10-negocio/catalogo-de-opciones.md`; `catastro` (**16**),
  `caja` (**3**) y `normativa` (**1**) las escriben a mano en su `CatalogoDelSistema` (ADR-0039
  §«Lo que cuesta», punto 5)—. Lo que este sistema guarda es **a quien se le concede cada una**, y
  desde la etapa 2 guarda ademas una **copia** de los cinco catalogos en
  `docs/10-negocio/catalogo-de-accesos/` para poder sembrarlos: es una copia y se dice, y lo que
  impide que se separe del original es la guarda cruzada de `infrastructure`. **Las cifras estan
  medidas el 2026-09-09 leyendo los cinco clones**, y la etapa 1 las tenia mal: decia 17, 4 y 2;
  y `rentas` dijo 134 durante las etapas 2 y 3 y dice **130** desde la 4, que le retiro las cuatro
  de administracion que ya no sirve.
  Las de **este** sistema son **siete** desde la etapa 3, y la septima —`eventos`— es la unica que
  no sale del manual: la piden las cuatro **cuentas de servicio** para leer y acusar el buzon.
- **NO esta en el camino caliente de ninguna autorizacion.** Cada sistema sigue autorizando contra
  **su copia local**: el `GuardiaDeAcceso` no cambia y `ComprobadorDeAccesoJdbc` sigue leyendo su
  propia base. Lo que cambia es **de donde sale esa copia**. Es lo que conserva la propiedad que
  `caja` existe para tener —«no le pregunta nada a nadie para cobrar […] es lo que hace cierto que
  la ventanilla cobre con `rentas` apagado»— y es el motivo por el que ADR-0039 descarto la salida
  1, que habria convertido «`rentas` caido» en «la ventanilla cerrada».
- **NO llama a ningun sistema hermano, y es una afirmacion.** Su egreso es DNS, su motor y
  Keycloak. La etapa 3 **no la cambia**, y es una decision: el buzon **se sirve** y no se empuja
  —empujar obligaria a conocer las cuatro direcciones, pedir cuatro credenciales y reintentar
  cuatro veces, o sea a que el dueno de la autorizacion dependa de que los cuatro esten arriba—.
  La etapa 4 tampoco, medido: quienes leen de aqui son los cuatro, y las aristas nuevas
  aparecieron en SUS descriptores —cada uno declara su egreso hacia `identidad-sistema`—, no en
  este.
- **NO se llama `seguridad`, nunca.** ADR-0033 reserva esa palabra para un sistema de *seguridad
  ciudadana*. Dos sistemas con ese nombre se descubren cuando alguien dimensiona el motor
  equivocado o abre el repositorio equivocado.
- **No decide la etiqueta de su imagen, ni su namespace, ni sus `PriorityClass`.** Las pone
  `infrastructure`.
- **No tiene `git log` de su historia**, porque no tiene historia: es el unico de los seis que no
  sale del corte del monolito. Lo que si se hereda son las decisiones, y estan enlazadas.

## Estructura

```
backend/                Gradle. Java 25, Spring Boot 4. Seis modulos
  kamayuk-identidad-dominio-compartido/  objetos de valor y contexto de tenant
  kamayuk-identidad-esquema/             V1__baseline.sql, V2 (el buzon) y la prueba de aislamiento
  kamayuk-identidad-plataforma/          token -> SET LOCAL -> RLS, y el patron de repositorio
  kamayuk-identidad-nucleo/              el contexto acotado: las once escrituras, los cuatro
                                         controladores, el buzon de salida y la implantacion
  kamayuk-identidad-seguridad/           el comprobador de acceso del guardia, y nada mas
  kamayuk-identidad-aplicacion/          ensambla el artefacto, y donde corren las barreras
infrastructure/         el descriptor de despliegue en TypeScript, con yarn
  src/descriptor.ts                      lo que este sistema aporta al cluster
  verificaciones/                        sus pruebas. NO son codigo de produccion
despliegue/compose.yaml la otra forma de levantarlo. Tiene que decir LO MISMO que el descriptor
docs/                   gobierno, el indice de ADR, los hallazgos de RLS, D0 y el catalogo de
                        accesos de los CINCO sistemas (`10-negocio/catalogo-de-accesos/`)
```

**No hay `frontend/` ni `infra/`, y las dos ausencias son afirmaciones.** No hay pantalla —las de
administracion viven hoy en `rentas` y su mudanza es otro issue (ADR-0039, etapas 2 y 3)— ni
guiones de carga de datos. El dia que los haya, su ruta entra en `RUTAS_DE_CODIGO` de
`docs/00-gobierno/verificar-fila-del-registro.mjs` **y su muestra en la autoprueba**, que es lo que
esa autoprueba exige: una ruta sin muestra no falla, **deja de comprobarse en verde**.

El backend **no compila sin `infrastructure` clonado al lado**: las barreras se consumen como
*composite build* desde `../../infrastructure/librerias-backend`. Y el descriptor **no instala sin
el**: su dependencia es `link:../../infrastructure/infra/contrato`, y eso lo resuelve yarn contra
el disco — ninguna variable de entorno lo redirige.

Los paquetes son `kamayuk.identidad.*`; los modulos, `kamayuk-identidad-<contexto>`. Los **roles de
base de datos son `kamayuk_owner`, `kamayuk_app` y `kamayuk_readonly`**. Son del **cluster**, que
los cinco sistemas comparten, asi que se renombran en los cinco a la vez o en ninguno: un
`crear-roles.sql` con el nombre nuevo y otro con el viejo dejan a uno de los dos sin poder
conectarse.

## La colision de nombre con Keycloak

**En la plataforma, `identidad` ya significa Keycloak.** Este repositorio no puede cambiarlo, asi
que aplica **una regla**: *donde el nombre chocaria con el `identidad` de Keycloak, este sistema
dice `identidad-sistema`*. Son dos sitios, y cada uno tiene su medida:

| Donde | Valor | Que pasaria con `identidad` |
|---|---|---|
| `componente` de sus pods (`infrastructure/src/descriptor.ts`) | `identidad-sistema` | `infra/descriptor/sistemas.ts:35` descarta `componente: identidad` del **grafo de egreso** como infraestructura: las cuatro aristas de la etapa 4 desapareceria y el grafo diria que a este sistema no lo llama nadie. No es un rojo: es un grafo que miente |
| El servicio del backend (`despliegue/compose.yaml`) | `identidad-sistema` | el alias de red `identidad` **lo tiene Keycloak** en `kamayuk-plataforma` (`plataforma.compose.yaml:106`), y los cinco backends le piden ahi su JWKS: con dos contenedores registrandolo, el DNS de Docker reparte y **todo token seria invalido de forma intermitente** |

La etiqueta `sistema: identidad` **no cambia** —la pone `infrastructure` en `commonLabels`— y es la
que dice de quien es el pod. **Y `componente: identidad` sigue siendo correcto como DESTINO**: es
como este descriptor nombra a Keycloak en su egreso, y hay una prueba que lo fija para que «no uses
`identidad`» no se cumpla dejando a este sistema sin poder traerse el JWKS.

**Lo que NO se cierra desde aqui**: que `grafoDeEgreso` distinga la plataforma por el **namespace
de destino** y no por el nombre de la etiqueta. Es un cambio en `infrastructure` (AC-7 del issue
#1). Y mientras `servicioDe()` de `compose-de-los-sistemas.ts` componga el servicio del backend
como el nombre del sistema, su guarda sale **roja** aqui diciendo «el compose no tiene ningun
servicio «identidad»» — un rojo ruidoso que nombra la decision, que se prefiere al DNS que reparte
entre dos contenedores porque **ese no se pone rojo en ningun sitio**.

## Antes de escribir codigo, leer

| Si vas a tocar… | Lee |
|---|---|
| Cualquier cosa | [ADR-0002 — Estrategia multi-tenant](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/adr/ADR-0002-estrategia-multi-tenant.md) — es el riesgo numero uno |
| Base de datos | [Los cinco hallazgos de RLS](docs/40-datos/hallazgos-de-rls.md) **primero**. Aqui el hallazgo 1 pesa el doble: lo aislado son los permisos |
| Por que existe este sistema | [ADR-0039](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/adr/ADR-0039-la-identidad-es-un-sistema.md) — entero, incluidas sus cinco etapas y lo que cuesta |
| La replica a los cuatro | [ADR-0028](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/adr/ADR-0028-el-tenant-no-cruza-por-http.md) §3, que es el buzon |
| Permisos de la sesion | [ADR-0013](https://github.com/hneyra/rentas/blob/main/docs/30-arquitectura/adr/ADR-0013-permisos-de-la-sesion.md), de `rentas`: la matriz se vuelve a pedir en cada renovacion |
| El descriptor | [ADR-0011](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/adr/ADR-0011-infraestructura-como-codigo.md) y [ADR-0031](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/adr/ADR-0031-infraestructura-comun-y-propia.md) §2 |
| El esquema | [ADR-0032](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/adr/ADR-0032-el-esquema-nace-en-baseline.md) — nace en baseline, sin historia que traer |
| Backend | [ARQ-04 — Estandares de codigo](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/estandares-de-codigo-backend.md) |
| Montar el entorno | [D0 — Desarrollo](docs/D0-desarrollo/README.md) |

Indice de decisiones: [`docs/30-arquitectura/adr/README.md`](docs/30-arquitectura/adr/README.md).

## Decisiones abiertas que bloquean

Registro completo en [GOB-02](https://github.com/hneyra/sgtm/blob/migracion-a-microservicios/docs/00-gobierno/decisiones-abiertas.md).

| # | Decision | Bloquea |
|---|---|---|
| ~~D-19~~ | **Contestada el 2026-09-09** en su parte de «quien es el dueno»: la autorizacion es un sistema propio y se replica por el buzon ([ADR-0039](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/adr/ADR-0039-la-identidad-es-un-sistema.md)). Sigue abierto **quien compone el catalogo de accesos de la sesion** | La sesion |
| D-22 | **Quien opera cinco despliegues.** Una municipalidad no opera cinco, y este sistema es el quinto | La implantacion |
| D-25 | **Si la separacion llega o no al hierro.** Cinco sistemas sobre un k3s de un nodo comparten disponibilidad — y `prod` **ya no cabia** con cuatro (#1) | El dimensionado |
| ~~—~~ | **Cuanto dura la ventana de inconsistencia** de la copia local: **medida el 2026-09-10** con las cinco aplicaciones levantadas, y escrita en [`identidad-4-la-ventana-de-la-copia-local.md`](https://github.com/hneyra/infrastructure/blob/main/docs/00-gobierno/identidad-4-la-ventana-de-la-copia-local.md) — `T_espera (0-300 s) + T_arranque + T_vuelta(N)`: **~2 min 36 s tipico y ~5 min 10 s en el peor caso**, con ~330 eventos/s en caliente. Es lo que ADR-0039 §«Lo que cuesta» punto 2 exigia medir y no suponer | ~~La etapa 5~~ |

## Reglas que no se negocian

Son las mismas en los seis repositorios, y las verifica **el mismo artefacto**:
[`comun-verificaciones`](https://github.com/hneyra/infrastructure/tree/main/librerias-backend/comun-verificaciones),
que vive en `infrastructure` y se consume como *composite build*.

| # | Regla | Motivo |
|---|---|---|
| 1 | **Importes en `BigDecimal`/`NUMERIC`.** Prohibidos `double` y `float` | Precision monetaria (RNF-055) |
| 2 | **Ningun metodo de dominio recibe `municipalidadId`.** Sale del token, se fija una vez con `SET LOCAL` | Si el desarrollador no lo maneja, no puede olvidarlo |
| 3 | **`SET LOCAL`, jamas `SET SESSION`** | `SET SESSION` sobrevive al retorno de la conexion al pool y contamina la peticion de otra municipalidad |
| 4 | **Sin `DELETE`** en deuda, pagos, recibos, valores, valuaciones, asientos ni auditoria. Se anula, se da de baja o se reversa | RNF-051, y el manual §Auditoria. **Aqui es la mas literal de las once**: un acceso no se borra, se revoca — si se borrara, la auditoria no podria decir quien lo tuvo |
| 5 | **Ningun literal numerico tributario en el codigo** | Reproducibilidad y cambio sin despliegue (RNF-053) |
| 6 | **Las reglas tributarias son funciones puras.** Sin base de datos, sin reloj, sin configuracion global; la fecha entra como argumento | Recalcular 2027 en 2037 debe dar el mismo centimo |
| 7 | **Nada de Spring ni JPA en la capa `dominio`** | Las reglas deben probarse sin levantar el contexto |
| 8 | **`alicuota`, nunca `tasa`**, para un porcentaje | `tasa` es un tipo de tributo |
| 9 | **No existe «la deuda»:** es `deudaActualizadaA(fecha)`, y toda cifra mostrada indica su fecha | RNF-075 |
| 10 | **Toda modificacion de datos exige observacion del usuario.** Sin observacion no se guarda | Manual §Auditoria; RNF-052. **Y aqui manda sobre cada concesion y cada revocacion**: quien concedio un permiso, cuando y por que |
| 11 | **Ningun SQL cruza la frontera de sistema** —un `JOIN` contra una tabla de otro sistema no deja huella en el bytecode, asi que lo vigila un escaner de texto y no ArchUnit | El dia que la base se parta, esa consulta deja de funcionar en produccion y no antes |
| 12 | **Ningun sistema que no sea `identidad` ESCRIBE la autorizacion** —`usuario`, `grupo`, `miembro`, `permiso`— (ADR-0039). **Aqui es el unico repositorio donde tiene que estar ENCENDIDA**, y lo esta desde la etapa 4: `escritoresDeLaAutorizacionConMotivo()` declara `AdministracionRepositoryJdbc` y `PermisoRepositoryJdbc`, **sin fecha de fin**, porque este sistema es el dueño | Dos sistemas que escriben la misma tabla de permisos en dos bases no dan un error: dan **dos respuestas** a «quien puede hacer esto» |

**La regla 12 nace desactivada en cada repositorio y aqui estuvo desactivada hasta la etapa 4**, con
su aviso impreso en cada corrida —«no se vigila en este repositorio … No es «no hay escrituras»: es
«no se ha mirado»»—. Declararla es lo que la enciende, y una entrada que no nombre una clase de
produccion la señala #27.

**Las reglas 1, 5, 6, 8 y 9 no tienen sujeto en este repositorio, y eso no las apaga**: aqui no hay
ni un importe ni una regla tributaria. Se conservan porque las verifica el mismo artefacto que los
otros cinco, y una regla que se quita «porque hoy no aplica» es la que nadie repone el dia que
aparece el primer importe.

**Si agregas una regla, agrega tambien la clase de muestra que la viola**, en las `muestras/` de
`comun-verificaciones`: una regla que no puede fallar no protege nada. Y lo exige por construccion
`ReglasDeArquitecturaMuerdenTest`, un `@TestFactory` sobre todas las reglas: una regla sin muestra
sale roja sola.

Lista completa con su justificacion:
[ARQ-04 — Estandares de codigo del backend](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/estandares-de-codigo-backend.md).

## Idioma

Español en el dominio, ingles en lo tecnico. **Sin tildes en identificadores**: Checkstyle lo
revisa en el backend, ESLint en el descriptor.

```java
public final class Acceso { … }                    // dominio: español
public interface AccesoRepository { … }            // patron: ingles
grupo.conceder(acceso, observacion);               // comportamiento: español
repository.findById(id);                           // infraestructura: ingles
```

Tablas y columnas en español `snake_case`. Campos de la API JSON en español `camelCase`.
Comentarios, pruebas y mensajes de commit en español.

## El monolito se llamaba `sgtm`, y en la prosa se sigue llamando asi

El producto es **Kamayuk**. El sistema del que sale —el monolito retirado— se llamaba `sgtm`, y
ese nombre **ya no esta en el codigo**: ni en un realm, ni en una imagen, ni en un identificador, ni
en un dato de configuracion.

**Pero sigue en los comentarios, en `docs/` y en el registro de «Verificar antes de afirmar», y eso
es deliberado.** No es limpieza pendiente:

- una fila del registro que dice «copiado de `sgtm@33f329a2`» **es la medicion que se hizo**;
  reescribirla la falsifica, y borrarla pierde con que rotura se demostro;
- un comentario que dice «hasta `E` la sonda apuntaba a `sgtm`» **es el motivo por el que el codigo
  de al lado es como es**; quitar el nombre lo deja sin sujeto y hay que volver a descubrirlo;
- y varias guardas explican en su docblock **de que defecto vienen**, que es lo que impide que
  alguien las «simplifique».

**Asi que NO se hace una pasada de limpieza sobre la prosa.** Si estas aqui por un `grep sgtm` que
devuelve cientos de lineas: casi todas son de este tipo y se quedan.

**Lo que si esta prohibido es que la cadena vuelva al codigo**, y lo vigila **una sola guarda para
los seis**: `sin-el-nombre-del-monolito.test.ts` de `infrastructure`, que barre este arbol y los
cinco clones hermanos. Barre **solo codigo de produccion** —ni `docs/`, ni `*.md`, ni pruebas— y
**omite comentarios**, por lo de arriba.

Esta en un sitio y no en `comun-verificaciones` porque, medido, **del lado Java no hay nada que
vigilar**: `backend/*/src/main` de los cinco solo nombra el monolito en comentarios y en dos
`COMMENT ON COLUMN`. Anadir una prohibicion a la libreria compartida exigiria su clase de muestra y
tocaria los seis builds para vigilar el conjunto vacio.

**Dos excepciones declaradas, y las dos con su motivo dentro de la guarda.** (1) Los buckets
`sgtm-{stg,prod}-respaldos` (`infra/Pulumi.{stg,prod}.yaml`): **son el nombre de cosas que
existen**, y renombrarlos en el codigo sin renombrar el bucket manda los respaldos a un sitio que no
existe — y eso no da error hasta el dia que hay que restaurar. (2) Dos `COMMENT ON COLUMN` dentro de
un `V1__baseline.sql` **ya aplicado**: Flyway valida la suma de comprobacion de cada migracion, asi
que editar una que ya corrio hace fallar el arranque de **toda base existente**. No es que no se
quiera cambiar: **no se puede** — se corregiria con una migracion nueva, si alguna vez importa.

## Comandos

```bash
cd backend
./gradlew verificarArquitectura   # ArchUnit, escaner de fuentes, aserciones y frontera de sistema
./gradlew verificarArranque       # el artefacto levanta en los dos perfiles (C-7). Requiere PostgreSQL 16
./gradlew verificarAislamiento    # aislamiento multi-tenant. BLOQUEANTE. Requiere PostgreSQL 16
./gradlew build                   # lo anterior mas Spotless
./gradlew spotlessApply           # arregla el formato en vez de solo reprocharlo

cd ../infrastructure
yarn install && yarn verificar    # el descriptor: lint, tipos y pruebas. Sin Pulumi ni cluster

# La plataforma: PostgreSQL con las bases, Keycloak con sus dos realms, Traefik y el buzon
cd ../../infrastructure
docker compose -f despliegue/plataforma.compose.yaml up -d --wait

# Y ESTE sistema contra ella: migraciones -> implantacion -> backend
cd ../identidad
docker compose -f despliegue/compose.yaml up --build --wait

# La guarda del registro y su autoprueba, que va ANTES
node docs/00-gobierno/verificar-las-muestras-del-registro.mjs
node docs/00-gobierno/verificar-fila-del-registro.mjs
```

**`verificarAislamiento` no se omite sin Docker: falla.** Una prueba bloqueante que se salta a si
misma deja el build en verde sin haber verificado nada. La salida documentada es apuntar a un
PostgreSQL 16 que ya exista, y **ninguna que omita la prueba**:

```bash
./gradlew verificarAislamiento \
  -Dkamayuk.pruebas.postgres.url=jdbc:postgresql://localhost:5432/postgres \
  -Dkamayuk.pruebas.postgres.usuario=postgres \
  -Dkamayuk.pruebas.postgres.clave=…
```

Tiene que ser **superusuario**, porque la prueba crea los roles del cluster, y **PostgreSQL 16**,
que es el motor que la plataforma levanta: verificar contra otro no dice nada del que corre. **El
esquema de este sistema no declara ninguna extension**, asi que la imagen `postgis` que el CI se
trae no la pide el esquema — la pide que el motor sea el mismo.

Como montarlo desde cero: [D0 — Desarrollo](docs/D0-desarrollo/README.md).

## Verificar antes de afirmar

**Ejecutar la prueba vale mas que razonar sobre ella.** Y no basta con que la verificacion este
escrita: **tiene que demostrarse que puede fallar** — se rompe a proposito el codigo que protege,
se ejecuta, y se anota el rojo exacto que sale.

Cada issue deja aqui una fila con que se implemento, **con que rotura se demostro que la
verificacion muerde** y que rojo produjo. Es lo que impide volver a descubrir el mismo hallazgo por
tercera vez.

> **La tabla nace vacia, y es correcto que se vea asi.** El registro anterior —288 filas, issue a
> issue— es historia de `sgtm` y **no viaja**: en un repositorio sin ese `git log` seria el
> registro de un trabajo que aqui no se hizo. Vive en
> [`sgtm/CLAUDE.md`](https://github.com/hneyra/sgtm/blob/migracion-a-microservicios/CLAUDE.md),
> que no se borra. Se consulta; no se copia.
>
> Y aqui la tabla nace vacia por partida doble: este es el unico de los seis repositorios que **no
> sale del corte del monolito**, asi que no hay ni siquiera un trabajo anterior al que referirse.
> La primera fila es la del issue #1.

Que la fila **exista** lo comprueba `docs/00-gobierno/verificar-fila-del-registro.mjs` en cada PR
que cierre un issue y toque codigo de produccion —`backend/<modulo>/src/main/`,
`infrastructure/src/` y `despliegue/` desde el primer dia, y
`docs/10-negocio/catalogo-de-accesos/` desde la etapa 2: los cinco catalogos viajan dentro del jar
y una opcion que se cae de uno deja su pantalla sin nadie que pueda dar permiso—. Lo que la fila **diga** —que
la mutacion sea real y las cifras cuadren— no lo puede leer una maquina: eso lo lee la revision.

| Verificacion | Como se demostro que puede fallar | Resultado |
|---|---|---|
| **#1 — el repositorio existe, verifica en CI y despliega como quinto sistema** (`backend/` con seis modulos y su baseline de 13 tablas; `infrastructure/src/descriptor.ts` con 21 pruebas; `despliegue/compose.yaml`; los cuatro flujos de CI; la guarda del registro con tres rutas de codigo y su autoprueba; y el PR hermano de `infrastructure` que lo compone) | **Diez roturas, cada una aplicada sola y restaurada por copia comparada con `cmp`** —«RESTAURADO IGUAL» las diez—: (R1) quitar `ALTER TABLE usuario FORCE ROW LEVEL SECURITY` del baseline; (R2) devolver las dos `UNIQUE` de `modulo_sistema` y `acceso` a `(municipalidad_id, codigo)`; (R3) que el arnes de la prueba de aislamiento intente conectar tambien con `rol_carga_parametros`; (R4) quitar `kamayuk-identidad-nucleo` de `SISTEMA_DEL_MODULO`; (R5) un `@RequiereAcceso(acceso = "usuarios")` en `src/main`; (R6) quitar el `@ExtendWith` de `ArquitecturaTest`; (R7) etiquetar los pods con `componente: identidad`, que es Keycloak; (R8) una etiqueta de imagen escrita a mano en el migrador; (R9) quitar `owner` del inventario de claves; y (R10) quitar de la autoprueba del registro la muestra que toca `despliegue/` | **1, 1 (initializationError), 1, 1, 1, 1, 2, 3, 1 y exit 1.** (R1) «usuario tiene FORCE ROW LEVEL SECURITY (sin esto, el propietario evade la politica): Expecting value to be true but was false» —la barrera numero uno, y la unica que se ejecuto ademas desde fuera del agente que la escribio—. (R2) «duplicate key value violates unique constraint "modulo_codigo_uq"»: `DatosDePrueba` siembra a proposito el mismo codigo para `identidad` y para `rentas`, que es el motivo de la **unica desviacion del DDL identico**: `modulo_sistema` y `acceso` ganan `sistema`, porque esta base guarda a quien se concede cada opcion de CUATRO catalogos y dos sistemas pueden nombrar igual dos opciones distintas. (R3) «FATAL: permission denied for database … User does not have CONNECT privilege»: `crear-roles.sql` crea los cuatro roles del cluster y **no** le da `CONNECT` a `rol_carga_parametros`, que es de `normativa`. (R4) «Expecting empty but was: `["kamayuk-identidad-nucleo"]`». (R5) `CatalogoDelSistemaTest` con «Expecting empty but was: `["usuarios"]`» y el remedio dentro: la prueba de los otros cuatro contrasta el catalogo contra los `@RequiereAcceso` y aqui hay **cero** endpoints, asi que copiada saldria roja acusando al catalogo de tener seis de mas; lo que afirma en su lugar es el estado de partida y caduca sola con el primer controlador. (R6) «Rule 'classes that son controladores HTTP …' failed to check any classes» — **este repositorio es la combinacion que `comun-verificaciones` no contempla: tiene dominio y no tiene capa web.** El unico permiso publicado, `sinContextosAcotadosTodavia()`, exige que tampoco haya dominio; se resuelve DENTRO de este repositorio con una extension que desactiva ese unico metodo de la base y aplica las mismas veinte reglas perdonando solo las cinco sin sujeto, contadas en las dos direcciones, y que cae sola con el primer controlador. Lo que falta en la libreria —el permiso por ambito— queda declarado. (R7) 2 en rojo, las dos de AC-7, y el resto verde: el defecto es solo del grafo. **La colision es real y esta medida**: `componente: identidad` es Keycloak en `infra/componentes/Identidad.ts` y `grafoDeEgreso` (`descriptor/sistemas.ts:35`) filtra esa etiqueta como infraestructura, asi que un sistema con ella desaparece del grafo; los pods de aqui llevan `componente: identidad-sistema` y el compose llama al backend igual, porque en la red `kamayuk-plataforma` el servicio `identidad` es Keycloak y dos alias iguales reparten el DNS. (R10) «`/^despliegue\//` no lo toca ninguna muestra que espere rojo». **Lo que la plantilla tenia y aqui no cabe, quitado con su motivo**: los dos modulos de negocio de `normativa`, sus dos pruebas de contrato (nadie consume nada de aqui todavia, y la entrada de Gradle tiene que volver con la primera operacion), `kamayuk.redondeo`, y las dos rutas sparse-checkout de `backend.yml`. **Y dos cosas que el encargo no decia y la medida corrigio**: `documento_emitido` tambien esta en los cuatro baselines (son 13 tablas comunes y no 12) y viaja con el paquete `documentos` de `plataforma`, asi que entra; y `nucleo` vacio **si** es un modulo para Spring Modulith 2 —el comentario heredado del SRTM que decia lo contrario salio falso al ejecutar `ModulosTest`—. **Cifras, medidas en esta maquina dos veces** (PostgreSQL 16.13 nativo en el 5432, JDK 25.0.4, `./gradlew build verificarArquitectura verificarAislamiento verificarArranque --rerun-tasks`): **488 pruebas, 0 fallos, 1 omitida** —la de la base que la extension desactiva—, `BUILD SUCCESSFUL` en 1 m 26 s; `cd infrastructure && yarn verificar` 21 de 21; la autoprueba del registro 9 muestras y 3 rutas. **Lo que no se pudo verificar aqui**: el compose no se levanto (sin demonio de Docker); la imagen no se construyo; y `yarn capacidad` con este sistema dentro es del PR hermano de `infrastructure`, que lo mide antes y despues |
| **Etapa 2 de [`infrastructure`#52](https://github.com/hneyra/infrastructure/issues/52) — la administracion llega, y cada escritura sale por el buzon en su misma transaccion** (`kamayuk-identidad-nucleo` deja de estar vacio: las **once escrituras** de `rentas@33f329a2` con sus dos repositorios y sus cuatro controladores bajo `/identidad/api/v1`, **37 archivos y 5 290 lineas** de `src/main`; `V2__buzon_de_identidad.sql` —203 lineas— con `identidad_evento`, **siete tipos**, `ENABLE`+`FORCE` RLS y **sin `UPDATE` ni `DELETE`** para `kamayuk_app`; `BuzonDeIdentidad`/`HechoDeIdentidad`/`BuzonDeIdentidadJdbc`, que **no declaran ninguna transaccion**; `docs/10-negocio/catalogo-de-accesos/` con los **cinco** catalogos —134·16·1·3·6 = **160** opciones— y `derivar-catalogo-de-rentas.mjs` que saca el de `rentas` del clon hermano; `ImplantarMunicipalidad` pasa por `AdministrarSeguridad`/`AdministrarPermisos` y `SembradorDeLaCopiaLocal` se queda en `SembradorDelCatalogo`; y las barreras vuelven a su forma: `class ArquitecturaTest extends ArquitecturaTestBase {}` — **488 -> 540 pruebas**, y de **1 omitida a 0**) | **Seis roturas, cada una aplicada SOLA y restaurada por copia comparada con `cmp`** —«RESTAURADO IGUAL» las seis—: **(R1)** quitar la emision de `registrarUsuario`; **(R2)** emitir con `@Transactional(propagation = REQUIRES_NEW)` y provocar el rechazo del ultimo administrador; **(R3)** devolver el `CHECK` cruzado de `V2` a la forma **negativa** y admitir un octavo tipo sin decidir su sujeto; **(R4)** que `ImplantarMunicipalidad` vuelva a escribir el administrador **por SQL directo**, como la etapa 1; **(R5)** apartar `catastro.json`, y aparte **con la guarda de Gradle neutralizada**; **(R6)** un `@RequiereAcceso` con una opcion que el catalogo no tiene | **1, 1, 1, 1, 1+3 y 1 en rojo — y TRES de las seis salieron primero en VERDE o apuntando a otro sitio, que es lo que hay que leer.**<br><br>**(R2) paso en VERDE las diez pruebas de `AdministrarPermisosTest`, «retirar el ultimo permiso … no deja rastro» incluida.** El motivo no era el codigo: era el arnes. `ArnesDeAdministracion` envolvia los casos de uso en un `TransactionInterceptor` de verdad y **al buzon no**, con un comentario que lo justificaba —«no declara ninguna transaccion a proposito»—, de modo que un `@Transactional` sobre el buzon **es un comentario**: nadie lo proxifica y la propiedad que sostiene el AC-2 no la podia medir nadie. Se envuelve igual que los demas —hoy no hace nada, porque `AnnotationTransactionAttributeSource` no encuentra ninguna anotacion— y con eso la misma rotura da 1 en rojo con la cuenta dentro: «[una escritura que se deshace se lleva su evento con ella (AC-2)] expected: 19L but was: 20L». Ese 20.º evento es la retirada del permiso del ultimo administrador **viajando a los otros cuatro sistemas mientras aqui el permiso sigue puesto**, y esa discrepancia no la corrige nadie.<br><br>**(R1) no sale donde el encargo decia, y la medida corrige el enunciado.** El encargo esperaba que la reconstruccion saliera distinta en `usuario`; medido, **`usuario` sale IDENTICO**: el cuerpo del evento es la fila ENTERA, asi que el `USUARIO_MODIFICADO` que emite `inhabilitarUsuario` unas lineas mas abajo **vuelve a crear la fila** con el estado final, que es justo lo que se compara. Donde sale es en **`miembro`**, y vacio: «expected: ["Caja|jperez|false|admin.emisor|admin.emisor|true", "Mesa de Partes|jperez|true|…", "Mesa de Partes|mlopez|true|…"] but was: []». O sea que un alta que no se emite **no se nota en su propia tabla si detras viene una modificacion**: se nota en lo que depende de ella. **Y ese rojo tampoco era el primero**: con el centinela de «los siete tipos se ejercieron» DELANTE de las cuatro comparaciones, el unico rojo era «could not find USUARIO_DADO_DE_ALTA» y **el entregable del AC-3 no llegaba a correr**. El centinela pasa DETRAS: existe para que un verde no sea vacio, no para explicar un rojo.<br><br>**Y (R1) destapo un defecto en el propio instrumento.** `AplicadorDeReferencia` aplica `miembro` y `permiso` con `INSERT ... SELECT ... FROM grupo g, usuario u WHERE ...`, que cuando lo que nombra no esta **no falla: escribe cero filas**. Con la emision quitada, el consumidor de referencia **se tragaba las tres afiliaciones sin una linea** y el rojo salia mucho despues, sobre una tabla vacia — un diagnostico que apunta a la tabla y no al evento. Es `rentas`#54 dentro del instrumento: quien aplica no puede descartar en silencio lo que no sabe colocar. Con la comprobacion puesta, la misma rotura dice la causa entera: «El evento de `miembro` nombra el grupo «Mesa de Partes» y la cuenta «jperez», y esta copia no lo conoce: la sentencia escribio 0 filas. Un evento que llega antes que aquel del que depende no se puede aplicar, y descartarlo en silencio deja la copia desatrasada sin que nada lo diga».<br><br>**(R3) hubo que medirla tres veces, y las dos primeras midieron otra cosa.** A la primera el rojo era «check constraint "identidad_evento_tipo_ck" … is violated by some row», lanzado desde el `finally`: la forma negativa **dejo entrar** la fila del octavo tipo —que es lo que se estaba midiendo— y devolver el `tipo_ck` fallaba por esa fila, **tapando la afirmacion**. El `finally` borra ahora lo que la rotura pudo colar. A la segunda el rojo era «Expecting code to raise a throwable», sin el `.as(...)`: `assertThatThrownBy` revienta antes de aplicar la descripcion cuando no se lanza nada, que es la leccion de `rentas`#40. Con `catchThrowable` el rojo dice el defecto entero: «[con el CHECK escrito en NEGATIVO esto entraria: la clausula habla de los siete que conoce y calla sobre el octavo, asi que un hecho sin sujeto se colaria con la forma del vecino y el consumidor recibiria un evento del que no puede decir de quien habla] Expecting actual not to be null».<br><br>**(R4) da el rojo entero de una vez**: «[la implantacion tiene que dejar los cuatro hechos del arranque en frio. Escribirlos con SQL directo —como hacia la etapa 1— deja la municipalidad implantada aqui y a los otros cuatro sistemas sin conocer a nadie, en silencio] Expecting ArrayList: [] to contain: ["GRUPO_DADO_DE_ALTA", "USUARIO_DADO_DE_ALTA", "MIEMBRO_AFILIADO", "PERMISO_FIJADO"]». **Las filas estan y el buzon esta vacio**, que es exactamente el estado que no se puede distinguir mirando pantallas.<br><br>**(R5) son dos mitades y la segunda encontro un tercer defecto.** La guarda de Gradle para el build antes de compilar y nombra el que falta: «Faltan en ../docs/10-negocio/catalogo-de-accesos los catalogos de: catastro. Sin ellos el jar sale sin las opciones de esos sistemas: nadie puede dar permiso a ninguna de sus pantallas (RF-122) … Si esto ocurre dentro de una imagen de Docker, el contexto de compilacion no incluye docs/». Y neutralizando la guarda —para medir que hace falta— **las cinco pruebas del catalogo unido y la de la implantacion salieron VERDES con el archivo apartado**: la tarea era un `Copy`, y **un `Copy` no borra del destino lo que desaparecio del origen**, asi que `build/generated/recursos/catalogo-de-accesos/catastro.json` seguia ahi —y de ahi al jar—. Pasa a ser un `Sync`, y con eso la misma rotura sin guarda da **3 en rojo** nombrando el archivo: «No se encontro /catalogo-de-accesos/catastro.json. Lo copia la tarea copiarCatalogoDeAccesos del build; sin el, la implantacion no tiene los accesos de 'catastro' que sembrar y nadie puede dar permiso a ninguna de sus pantallas (RF-122)». Las dos mitades hacen falta: la guarda para que el build no salga, el `Sync` para que si alguien la quita el jar no lleve una copia rancia.<br><br>**(R6) da 1 en rojo con los dos conjuntos enteros**: «to contain exactly (and in same order): ["accesos", "grupos", "grupos_que_el_catalogo_no_tiene", …] but could not find … ["grupos_que_el_catalogo_no_tiene"]». `CatalogoDelSistemaTest` vuelve a la forma de los otros cuatro —comparacion en los dos sentidos entre los `@RequiereAcceso` de `src/main` y el catalogo— y gana ademas la mitad de la etapa 2: que **`identidad.json` diga lo mismo que la clase**.<br><br>**El `sistema` es la unica desviacion respecto de `rentas`, y esta en todas partes porque el esquema la obliga.** `modulo_sistema` y `acceso` se llavean por `(municipalidad_id, sistema, codigo)`: aqui conviven los catalogos de los cinco, `permisos` es una opcion de `identidad` **y** de `rentas`, y `SEGURIDAD` es un modulo de tres. Asi que `Acceso`, `Modulo` y `PermisoEfectivo` llevan el sistema; `accesoPorCodigo(sistema, codigo)` lo exige y `modulos`/`accesos` lo aceptan **nulo** —que son los cinco—; los cuerpos del `PUT` de permisos lo piden y **sin valor por omision** (422 enumerando los cinco), porque un sistema supuesto seria dar permiso sobre la opcion de otro; y la guarda del ultimo administrador cuenta por el **par**, con su prueba: dar los siete privilegios sobre `rentas:permisos` **no** convierte a nadie en administrador de `identidad`.<br><br>**Cifras, medidas en esta maquina** (PostgreSQL 16.13 nativo en el 5432, JDK 25.0.4, `./gradlew build verificarArquitectura verificarAislamiento verificarArranque --continue --rerun-tasks --max-workers=2 --no-daemon`): la linea base se tomo en un **worktree limpio de `89eca30`** —no en el arbol de trabajo, donde la primera medida salio contaminada porque ya habia archivos copiados— y da **488 pruebas, 0 fallos, 1 omitida**; esta rama da **540, 0 fallos, 0 omitidas**, `BUILD SUCCESSFUL in 1m 45s`. El **+52** es **+53 nuevas menos 1 que se retira**: `nucleo` pasa de 0 a **51**, `seguridad` de 7 a **9**, y `aplicacion` de 99 a **98** porque la omitida era `lasVeinteReglasCorrenYLasQueNoTienenSujetoSeDicen`, la extension `SinCapaWebTodavia` de la etapa 1, **que caduco sola como su propio mensaje decia**. `minimoDeFuentesDeProduccion` 100 -> **130** (medido 143) y `minimoDePruebas` 40 -> **55** (medido 70). `node docs/10-negocio/derivar-catalogo-de-rentas.mjs --comprobar` dice «El catalogo de `rentas` cuadra: 134 opciones en 12 modulos», y la autoprueba del registro, «Las 11 muestras se comportan como deben, y las 4 rutas declaradas como codigo de produccion tienen quien las ejerza».<br><br>**Lo que NO se cierra, con su motivo.** (a) **`rentas` no cambia** (AC-7): conserva sus once escrituras hasta la etapa 4, asi que entre esta etapa y aquella **hay dos sitios que administran**, y su `rentas.json` declara las 134 —las once de Seguridad incluidas— porque la guarda cruzada del PR hermano compara contra el catalogo real de cada clon. (b) **El buzon no tiene consumidor**: `pendientesDesde` esta y no la llama nadie; el acuse es **por consumidor** —son cuatro— y por eso `identidad_evento` no lleva `estado` ni `marcarEntregados`: eso es la etapa 3. (c) **`ImplantarMunicipalidad`, `DatosDeImplantacion`, `SembradorDelCatalogo` y `RegistroDeMunicipalidadesJdbc` se mudaron a `nucleo`**, y es una desviacion del encargo: `nucleo.aplicacion` es un sub-paquete y Spring Modulith no lo expone, asi que desde `seguridad` no se podia llamar; la alternativa era una `@NamedInterface`, o sea una dependencia del framework dentro de un contexto acotado. (d) **El compose no se levanto ni la imagen se construyo**: sin demonio de Docker en esta maquina. Lo unico que se pudo ejercer del `Dockerfile` es que **el catalogo entra en el contexto** —`.dockerignore` lo exceptua y el `COPY` lo trae—, y sin eso `exigirCatalogoDeAccesos` haria fallar la imagen, que es lo que su mensaje dice. (e) **La guarda cruzada de los cinco catalogos y el escaner de las cuatro tablas son del PR hermano de `infrastructure`** (AC-4 y AC-5): de este lado quedan los cinco JSON con su forma estable y las clases que escriben, nombradas en el informe |
| **Etapa 3 de [`infrastructure`#52](https://github.com/hneyra/infrastructure/issues/52), la mitad de `identidad` ([#3](https://github.com/hneyra/identidad/issues/3)) — el buzon se SIRVE, y un acuse es de un consumidor y no de todos** (`V3__acuses_del_buzon.sql`: `identidad_evento_acuse` con el consumidor DENTRO de la clave primaria, `CHECK` de los cuatro, RLS `ENABLE`+`FORCE` e `INSERT, SELECT` y nada mas; `Consumidor` —el enumerado que se construye del `azp` y rechaza todo lo demas—, `BuzonDeIdentidad.pendientesPara`/`acusar` con su `Lote` y su `EventoQueNoConsta`, `EntregaDeEventos` y `EventosController` bajo `Api.RAIZ`; la opcion `eventos` en `CatalogoDelSistema` y en `identidad.json` —**6 → 7**, y el catalogo unido **160 → 161**—; el grupo «Consumidores del buzon» que la implantacion siembra con esa unica opcion y **sin miembros**; `SIN_IDENTIDAD_DE_SERVICIO`, un codigo de error propio; y las cuatro pruebas de contrato con lo que las sostiene — **546 → 577 pruebas, 8 omitidas**) | **Siete roturas, cada una aplicada SOLA y restaurada por copia comparada con `cmp`** —«RESTAURADO IGUAL» las siete—: **(R1)** la clave primaria del acuse **sin `consumidor` dentro**; **(R2)** que el consumidor deje de salir del `azp` y se fije desde el codigo, que es lo mismo que leerlo de un `?consumidor=`; **(R3)** quitar el `ON CONFLICT DO NOTHING`; **(R4)** `pendientesPara` **sin `ORDER BY e.id`** —con `ORDER BY e.creado_en DESC`, para que se note—; **(R5)** apartar el clon de `rentas`; **(R6)** un clon de `rentas` que **si** publica su contrato con `identidad`; y **(R7)** quitar la entrada de Gradle que declara los contratos de los hermanos | **1, 4, 1, 1, 2, 1 y VERDE en rojo.**<br><br>**(R1) muerde, y NO por donde el propio `V3` decia, que es lo que hubo que corregir.** La cabecera de la migracion explica el defecto como «el primero que acusa retira el evento para los otros tres»; medido, con la clave `(municipalidad_id, evento_id)` eso **no** es lo que pasa —el `LEFT JOIN` filtra por `a.consumidor`, asi que `caja` sigue viendo el evento— y lo que pasa es la otra cara: el `ON CONFLICT DO NOTHING` **se traga el acuse de `caja` en silencio**, su cola no se mueve y cada vuelta le sirve lo mismo, para siempre. La primera version del rojo era **«expected: 1 but was: 0» a secas**, sin una palabra de por que, asi que se le escribio su `.as(...)` y ahora dice el defecto entero antes de la cifra.<br><br>**(R2) da 4 en rojo y el primero es el que este controlador existe para producir**: «expected `[0ac39d9c…, 45bd3978…]`… could not find `[0ac39d9c…]`» — `rentas` pide con SU token y ha perdido el evento que acuso `caja`. Los otros tres son los tres tokens que **no** son una cuenta de servicio —el del backoffice, ninguno, y el de `identidad`— pasando a **200** donde tienen que dar 403: sin el `azp`, «quien pregunta» deja de ser una pregunta.<br><br>**(R3)** da `DuplicateKeyException … "identidad_evento_acuse_pk"`: un consumidor que confirmo su transaccion y perdio el acuse volveria a aplicarlo en cada vuelta para siempre. **(R4)** no da una diferencia de tablas sino algo mejor: revienta al APLICAR, «El evento de `miembro` nombra el grupo «Caja» y la cuenta «jperez», y esta copia no lo conoce: la sentencia escribio 0 filas» — el orden **es** contenido, y una afiliacion que llega antes que el alta de su grupo no se puede aplicar. Y el desempate no es teorico: con el reloj fijo del arnes los siete eventos comparten `creado_en` al milisegundo, asi que ordenar por el instante deja el orden **al planificador** —medido aparte contra PostgreSQL 16.13: `ORDER BY creado_en DESC LIMIT 3` sobre veinte filas empatadas devuelve `2,3,4,5,6,1`—.<br><br>**(R7) es el hallazgo, y paso en VERDE.** Sin la entrada de Gradle, publicar el contrato en el clon hermano deja `> Task :kamayuk-identidad-aplicacion:test **UP-TO-DATE**` y `BUILD SUCCESSFUL`: la guarda no se pone roja, **deja de correr**. Es #192 §2 otra vez, medido en las dos direcciones —con la entrada puesta y tocando SOLO el archivo del hermano, la tarea vuelve a ejecutarse y falla—. Se declara el **directorio** y no el archivo a proposito: hoy ese archivo **no existe**, y un archivo que no existe no tiene contenido que cambiar.<br><br>**La decision que hubo que medir antes de tomarla: las cuatro pruebas de contrato NO pueden quedar activas hoy.** El encargo ofrecia usar `desajustesVivos()` para declarar «el consumidor todavia no publica su contrato» como deuda con nombre; medido escribiendo una y corriendola, **no se puede**: `ContratoDelConsumidor.leer` **lanza** antes de que la base consulte ese gancho —«No esta «…/rentas/docs/50-api/contratos-que-consume/identidad.json», asi que no se puede comprobar…»—, asi que las cuatro saldrian rojas y el build con ellas. Van con `@Disabled` **y** con lo que las reactiva: `ContratosDeLosConsumidoresTest` afirma que el archivo **sigue sin existir** en los cuatro clones, y **(R6)** lo mide —«uno o mas consumidores YA publican… hay que quitarle el `@Disabled` a `ContratoCon<Consumidor>Test`»—. Sus otras dos comprobaciones son las que impiden que se cumpla sola: **(R5)** da 2 en rojo, «Expecting empty but was: `["rentas"]`» y el contraste de la ruta con los tres contratos de ese clon nombrados uno a uno. Y por eso `backend.yml` gana los **cuatro** `sparse-checkout` **ahora y no en la etapa 4**: sin los clones, «nadie publica» seria cierto porque no existe el repositorio, y la guarda se cumpliria sola para siempre.<br><br>**Y una guarda que ya existia enseño donde NO se puede escribir el codigo de acceso.** `EventosController` nacio con `@RequiereAcceso(acceso = ACCESO, …)`, una constante privada; compila igual y `CatalogoDelSistemaTest` **lee el fuente**, asi que el escaner encontro seis codigos donde el catalogo declara siete y la prueba acuso al **catalogo** de tener uno de mas —«some elements were not expected: `["eventos"]`»—, que manda a mirar lo contrario de lo que pasa. Va como literal en las dos anotaciones, con su motivo escrito.<br><br>**Tres decisiones con su motivo, y ninguna es de gusto.** (1) **`eventos` es una opcion PROPIA** y no una de las seis de administracion, al reves de lo que hizo `catastro` —que sirvio su buzon con `consulta_fichas` para no crear una opcion que nadie abre—: alli el buzon lleva el padron, o sea lo mismo que esa opcion ya deja leer; aqui lleva **quien puede hacer que**, y darlo con `usuarios` o con `permisos` significaria que todo administrador puede ademas vaciarle la cola a `caja`. (2) **`SIN_IDENTIDAD_DE_SERVICIO` es un codigo propio y no `SIN_PRIVILEGIO`**, por el mismo criterio con que existen `METODO_NO_ADMITIDO` y `MARCO_CON_DEMASIADOS_LOTES`: los dos se arreglan al reves —«no tiene el privilegio» se arregla concediendoselo, y esto se arregla pidiendo otro token—, y con el mismo codigo un administrador que probara el buzon desde el backoffice se concederia a si mismo un permiso que no le sirve. (3) **El ubigeo del `azp` NO se compara con el del contexto**: el inquilino lo fija `TenantContextFilter` desde el claim `municipalidad_id` del **mismo** token firmado (ADR-0005), asi que compararlos seria comprobar un token consigo mismo por un camino mas largo y anadir una segunda fuente de la verdad del inquilino.<br><br>**Y el acuse es la primera escritura de este sistema que entra en `escriturasSinUsuarioQueObserve()`**, con su motivo: la regla 10 existe porque «el que» lo reconstruye cualquier sistema y «el por que» solo lo sabe quien cambio el dato; aqui no cambia ningun dato del padron y no hay ninguna persona detras — lo que se escribe es un acuse de recibo, y sus cuatro columnas **son** el registro completo del acto. Exigir una observacion produciria la cadena fija que el javadoc de la regla advierte. La entrada la vigila `SujetosDeLaConfiguracion` (#27) en las dos direcciones.<br><br>**Cifras, medidas en esta maquina** (PostgreSQL 16.13 nativo en el 5432, JDK 25.0.4, `./gradlew build verificarArquitectura verificarAislamiento verificarArranque --continue --rerun-tasks --max-workers=2 --no-daemon`): **546 → 577 pruebas, 0 fallos, 0 errores**, `BUILD SUCCESSFUL in 1m 36s`. Las **+31** son todas de este trabajo —`nucleo` 51 → **70** (6 de `ConsumidorTest`, 6 de `AcusesPorConsumidorTest`, 6 de `BuzonServidoDePuntaAPuntaTest` y 1 de `ReconstruccionPorLoQueSeSirveTest`) y `aplicacion` 102 → **120** (4 de `ContratosDeLosConsumidoresTest` y las **8 omitidas**, que son las cuatro clases desactivadas con sus dos metodos)—. **Y la linea base hubo que medirla DOS veces, que es parte de lo medido**: la primera dio **540** y esa cifra ya no es de nadie —`comun-verificaciones` se consume por *composite build* desde el clon de `infrastructure`, y el otro carril de esta misma etapa le estaba anadiendo el escaner de AC-5 mientras esto corria: seis pruebas nuevas de `ProhibicionesEnElCodigoFuenteTest` («el escaner detecta la muestra que escribe la autorizacion», «el interruptor abre y cierra: sin declarar nada, esta prohibicion no revisa», y cuatro mas) que **no son de este trabajo**—. La buena se tomo sobre un arbol extraido de `c298b47` con `git archive`, en la misma maquina y contra la misma libreria: **546, 0 fallos, 0 omitidas**. Una linea base se mide contra lo mismo que usara quien la lea, y aqui «lo mismo» incluye el clon hermano.<br><br>**Lo que NO se hace, con su motivo medido.** (a) **No se toca ninguno de los cuatro consumidores**: el ingestor, su `CredencialDeServicio` y su `contratos-que-consume/identidad.json` son la etapa 4, y cada uno es un PR de su repositorio. (b) **No se copia `TokenDeServicioDeKeycloak`** (AC-5 del encargo): este sistema **no llama a nadie** —sirve el buzon, no lo empuja—, asi que no tiene a quien pedirle un token; quien lo necesita es el consumidor. (c) **`docs/50-api/formas-de-la-api.json` no se actualiza porque NO EXISTE, y la premisa del encargo era falsa**: medido, `FormasDeLaApiTest` vive **solo** en `rentas` —deriva del contrato OpenAPI que su generador produce del prototipo del manual (#312)— y ni `catastro`, ni `normativa`, ni `caja`, ni este repositorio tienen `docs/50-api/`. Inventar aqui un contrato a mano es exactamente lo que #312 prohibe. Lo que fija la forma de las dos operaciones es `BuzonServidoDePuntaAPuntaTest.laFormaDelEventoServido`, campo a campo, y esta escrita en el informe para que la etapa 4 la copie. (d) **El grupo «Consumidores del buzon» nace sin miembros**: afiliar las cuatro cuentas de servicio —`service-account-kamayuk-<sistema>-servicio-<ubigeo>`— exige que existan en el emisor y tengan fila en `usuario`, y las dos cosas son del despliegue. Hoy el guardia les contestaria «la cuenta no esta dada de alta en este sistema», que es lo correcto. (e) **Nada de esto se ha ejercido contra las cinco aplicaciones levantadas**: no hay demonio de Docker en esta maquina. Lo que si esta medido es de HTTP a PostgreSQL con MockMvc y el token puesto en el contexto de seguridad, que es lo que Spring Security deja cuando ya lo valido |
| **Etapa 4 de [`infrastructure`#52](https://github.com/hneyra/infrastructure/issues/52), la mitad de `identidad` ([#4](https://github.com/hneyra/identidad/issues/4)) — los cuatro consumidores existen, sus cuatro contratos se comprueban AQUI, y `rentas` dejo de administrar** (las cuatro `ContratoCon<Consumidor>Test` pierden su `@Disabled` y corren contra los cuatro `docs/50-api/contratos-que-consume/identidad.json` de los clones hermanos —`rentas@d4c3fb5`, `catastro@76a4833`, `normativa@909724f`, `caja@5eacae1`—; `ContratosDeLosConsumidoresTest` da la vuelta a sus dos afirmaciones —de «ninguno publica y las cuatro llevan `@Disabled`» a «los cuatro publican y ninguna lo lleva»— y lee el fuente **sin comentarios**, porque el javadoc de cada una nombra la anotacion para explicar por que ya no la lleva; `catalogo-de-accesos/rentas.json` regenerado con `derivar-catalogo-de-rentas.mjs`, **134 → 130** y el catalogo unido **161 → 157**, con las cifras subidas a mano en `CatalogoUnidoTest`, `AdministrarSeguridadTest`, `build.gradle.kts` de `nucleo`, `CatalogoUnidoDelJar`, `ImplantarMunicipalidad`, `CatalogoDelSistema` y el derivador; y todo lo que decia «entre esta etapa y la 4 hay dos sitios donde se administra» corregido — `package-info` de `nucleo` y de `seguridad`, `EventosController`, README, descriptor y esta tabla. **577 → 577 pruebas, y de 1 fallo y 8 omitidas a 0 y 0**) | **Cuatro roturas, cada una aplicada SOLA y restaurada por copia comparada con `cmp`** —«RESTAURADO IGUAL» las cuatro, y las dos que tocan un clon hermano se hicieron sobre COPIAS privadas de su `docs/50-api`, con los cinco clones de `e4/` en `git status` vacio antes y despues—: **(M1)** apartar el `identidad.json` del clon de `caja`; **(M2)** devolver el `@Disabled` a `ContratoConCajaTest`; **(M3)** anadir a mano `eventos[].sujetoNombre` al contrato de `catastro` —un campo que su ingestor leeria y este servidor no publica— y mirar si la tarea de prueba **vuelve a correr** o queda `UP-TO-DATE`; y **(M4)** dejar `rentas.json` en 134 con el clon de `rentas` en 130 | **2, 1 (+2 omitidas), 1 y 1+1 en rojo — y la linea base ya era el rojo que este trabajo existe para producir.** <br><br>**La linea base se midio antes de tocar nada**, sobre un arbol extraido con `git archive a0866be` a un directorio privado con los cinco clones hermanos enlazados al lado (PostgreSQL 16.13 nativo en el 5432, JDK 25.0.4, `./gradlew build verificarArquitectura verificarAislamiento verificarArranque --continue --rerun-tasks --max-workers=2 --no-daemon`): **577 pruebas, 1 fallo, 8 omitidas**, `BUILD FAILED in 1m 31s`, y el fallo es la guarda de la etapa 3 haciendo lo suyo: «[esta es la lista de trabajo de la etapa 4, y este rojo es su remedio] uno o mas consumidores YA publican lo que le piden a este buzon, asi que su prueba de contrato ya se puede correr: hay que quitarle el @Disabled a ContratoCon<Consumidor>Test … **Expecting empty but was: ["rentas", "catastro", "normativa", "caja"]**». Los cuatro a la vez, porque los cuatro consumidores se construyeron el mismo dia. Las 8 omitidas son las cuatro clases con sus dos metodos. **Y la primera medicion no valio, que es parte de lo medido**: el `../../base.log` de aquel comando aterrizo en otro sitio y lo que se leyo primero fue un `base.log` de otro carril, fechado a las 12:16, con un arbol de la etapa 1 dentro (`nucleo:test NO-SOURCE`, siete pruebas en `seguridad`); lo delato que sus rutas eran de `/home/user/identidad` y no del arbol extraido. Los XML del arbol propio son los que cuentan. <br><br>**Con las cuatro activas, los cuatro contratos CUMPLEN, y eso se midio antes de dar la vuelta a nada**: 12 de 12 en verde. Los cuatro `identidad.json` son identicos en forma —`raiz` `/identidad/api/v1`, `GET /eventos/pendientes` con `limite` y los siete campos del evento mas `quedan`, `POST /eventos/acuses` con `eventos: [texto]` y `recibidos`/`escritos`/`quedan`—, o sea exactamente lo que `BuzonServidoDePuntaAPuntaTest.laFormaDelEventoServido` fijo en la etapa 3 para que la 4 lo copiara. **Y una premisa del encargo salio falsa en la direccion buena**: sospechaba que `catastro` y `normativa` declararian otra raiz; los cuatro archivos dicen `/identidad/api/v1`, asi que **no hay ningun desajuste real que reportar** entre lo que un consumidor lee y lo que este servidor publica. <br><br>**(M1) da 2 y los dos nombran a `caja`**: `ContratoConCajaTest` con «No esta «…/caja/docs/50-api/contratos-que-consume/identidad.json», asi que no se puede comprobar que este backend siga cumpliendo lo que su consumidor espera» y la guarda con «[este rojo cae AQUI y no en el consumidor, y por eso dice de quien es] … Remedio: en el clon del consumidor, regenerar el contrato con su generador (ContratoQueConsumeDeIdentidad) … **Expecting empty but was: ["caja"]**». Hacen falta las dos: la primera dice que no se pudo comparar, la segunda de quien es el archivo y como se repone. **(M2) es la que justifica que la guarda siga existiendo con las cuatro activas**: con el `@Disabled` de vuelta, `ContratoConCajaTest` sale **SKIPPED** dos veces y **el build seguiria en verde** —una prueba desactivada no falla, se salta—; lo unico que cae es «**ContratoConCajaTest.java: lleva @Disabled, y el contrato de caja existe**: una prueba desactivada no falla, se salta, y el build sigue en verde mientras este buzon deja de publicar lo que ese ingestor lee». Y la primera version de esa guarda **se habria puesto roja sola**, porque el javadoc de las cuatro dice `@Disabled` para contar por que ya no lo llevan: se lee el fuente sin comentarios (#42, #16). <br><br>**(M3) mide la entrada de Gradle de la etapa 3, y en las dos direcciones.** Primera corrida de `ContratoConCatastroTest`, verde; segunda sin tocar nada, `> Task :kamayuk-identidad-aplicacion:test UP-TO-DATE` (el contraste); tocado el contrato del clon —el directorio esta declarado como entrada desde la etapa 3—, la tarea **vuelve a ejecutarse** y falla nombrando el campo: «GET /eventos/pendientes: **falta el campo «eventos[].sujetoNombre»**, que el consumidor lee. Este endpoint declara [creadoEn, cuerpo, eventoId, huella, secuencia, sujetoId, tipo].». Es #192 §2 medido con el archivo existiendo, que la etapa 3 solo pudo medir con el archivo ausente. <br><br>**(M4) da un rojo por cada lado del catalogo**: `node docs/10-negocio/derivar-catalogo-de-rentas.mjs --comprobar` sale con **exit 1** —«…/rentas.json no cuadra con el catalogo de `rentas`. Regenerarlo: node docs/10-negocio/derivar-catalogo-de-rentas.mjs»— y `CatalogoUnidoTest` con «[el catalogo de «rentas» trajo otra cantidad de opciones. O el archivo cambio —y hay que remedirlo aqui y en la guarda cruzada de `infrastructure`— o se leyo a medias] expected: 130L but was: 134L». El `Sync` de la etapa 2 hizo lo suyo: el jar llevo la copia de 134 y la prueba la vio. Y el estado de partida de esta rama **era** M4: `a0866be` tenia `rentas.json` en 134 con `rentas@d4c3fb5` en 130, y `--comprobar` ya salia rojo antes de tocar nada. <br><br>**Lo que la medida corrigio del encargo: `rentas` retiro CUATRO opciones y no once, y con ellas se fue el homonimo con que se media el par.** El encargo hablaba de «las once escrituras» y de once opciones de Seguridad; medido sobre `rentas/docs/10-negocio/catalogo-de-opciones.md`, se fueron `usuarios`, `grupos`, `miembros` y `permisos` (134 → 130, y no 123) y `rentas` **conservo** `modulos` y `accesos` ademas de las otras cinco, porque son lecturas de su copia local que su interfaz pide para componer el menu. Consecuencia que no venia en el encargo: **tres pruebas de `nucleo` se pusieron rojas al regenerar el catalogo** —`CatalogoUnidoTest.elCodigoSoloNoIdentifica`, «Expecting actual: ["identidad"] to contain exactly: ["identidad", "rentas"]», y las dos de `AdministrarPermisosTest` que fijaban permisos sobre `rentas:permisos`, «No hay ningun acceso 'permisos' del sistema 'rentas'»—, porque las tres usaban `permisos` como ejemplo de «el mismo codigo en dos sistemas» y desde la etapa 4 ningun catalogo real tiene otro `permisos`. Los homonimos que quedan son `modulos` y `accesos`: la prueba del catalogo afirma **los dos** y la de las dos filas usa `accesos`. La guarda del ultimo administrador cuenta el par `(identidad, permisos)`, y para medir que cuenta por el par y no por el codigo hace falta un homonimo que ya no existe: se **siembra en la propia prueba** con el mismo `SembradorDelCatalogo` de la implantacion, dicho en su javadoc, porque el defecto que vigila es el mismo aunque hoy no haya con que confundirse, y el dia que otro sistema estrene una opcion con ese codigo es justo cuando no se puede descubrir. <br><br>**Lo que contesta hoy el buzon a una cuenta de servicio sin fila en `usuario`, medido y no supuesto**: el grupo «Consumidores del buzon» sigue naciendo **sin miembros** —afiliar las cuatro cuentas es del despliegue, y la etapa 4 no lo cambio—, y lo que el guardia contesta lo fija `GuardiaDeAccesoTest.sinFichaLoDice` de la plataforma: **403 `SIN_PRIVILEGIO`** con «La cuenta «…» no esta dada de alta en este sistema. No es que le falte un privilegio: no tiene ninguna ficha aqui, y el alta no se hace desde este sistema — la administracion de usuarios, grupos y permisos vive en rentas». **La segunda mitad de esa frase es falsa desde la etapa 4 en los cinco sistemas** —vive aqui—, y no se toca desde este repositorio: `GuardiaDeAcceso` es de `plataforma`, una de las piezas que `lo-que-los-cinco-comparten` compara byte a byte, y arreglarla en una copia es sumar una divergencia; se corrige en la pieza compartida (`kamayuk-lib`, ADR-0038). Queda dicho en el javadoc de `EventosController`. <br><br>**Cifras, medidas en el mismo entorno y con el mismo comando**: **577 → 577 pruebas**, y lo que cambia es **de 1 fallo y 8 omitidas a 0 y 0**, `BUILD SUCCESSFUL in 1m 58s`; por modulo, `aplicacion` 116+4, `dominio-compartido` 154, `esquema` 43, `nucleo` 70, `plataforma` 181 y `seguridad` 9, todos con 0 fallos. El total no se mueve porque las ocho de las cuatro clases ya contaban como omitidas: lo que este trabajo compra son ocho pruebas que corren donde antes se saltaban, y cuatro contratos comparados donde antes no habia nada que comparar. `cd infrastructure && yarn verificar` **21 de 21**; `node docs/10-negocio/derivar-catalogo-de-rentas.mjs --comprobar` dice «El catalogo de `rentas` cuadra: 130 opciones en 12 modulos». <br><br>**Lo que NO cierra, con su motivo.** (a) **La guarda cruzada de `infrastructure` (`catalogo-de-accesos.test.ts`) sigue diciendo 161 y `rentas: 134`**, escrito a mano como manda su propio comentario; con `rentas@d4c3fb5` y este `rentas.json` en 130 se pondra roja por los dos lados hasta que se remida alli a **157 y 130**, y su fila de la etapa 4 ya lo deja escrito. Es de ese repositorio y no se toca ningun arbol hermano. (b) ~~**El grupo «Consumidores del buzon» sigue sin miembros**~~ — **esto se midio y era el defecto: lo cierra el segundo commit de esta misma rama**, mas abajo. (c) **La ventana de inconsistencia sigue sin medirse**: ahora ya hay quien la produzca —los cuatro ingestores— y medirla exige las cinco aplicaciones levantadas, que es la etapa 5 y no cabe en esta maquina sin demonio de Docker. (d) **Nada de esto se ejercio contra un consumidor de verdad**: lo medido es que los cuatro archivos que esos consumidores generan de su adaptador cuadran con lo que este controlador publica, por reflexion sobre los `record` y no por HTTP. (e) **El README describe el estado de la etapa 1 en su tabla**, y aqui solo se corrigio el parrafo que decia «el defecto sigue vivo hasta la etapa 4»; reescribir esa tabla es otro trabajo, y el estado medido fila a fila vive en esta tabla. |
| **La misma etapa 4, segundo commit ([#10](https://github.com/hneyra/identidad/pull/10)) — la implantacion da de alta las cuatro cuentas de servicio y las afilia, que es lo que hacia que los cuatro consumidores no pudieran leer el buzon** (`Consumidor.clienteDeServicio`/`cuentaDeServicio`, las dos direcciones de la forma de ADR-0028 §2 en la misma clase que ya la analiza; `ImplantarMunicipalidad.grupoDeConsumidoresDelBuzon` siembra los **cuatro** de `Consumidor.values()` como `usuario` y los afilia a «Consumidores del buzon», por `AdministrarSeguridad` y emitiendo sus eventos; `ElBuzonAutorizaALosConsumidoresTest`, la prueba de punta a punta con el **guardia de produccion y `ComprobadorDeAccesoJdbc` de verdad** delante de `EventosController`; y `ImplantacionEmiteSusEventosTest` invertido —de «nace SIN MIEMBROS» a las cuatro cuentas por su nombre— con la cuenta de eventos que la implantacion emite. **577 → 582 pruebas**, `nucleo` 70 → 75) | **Tres roturas, cada una aplicada SOLA sobre `src/main` y restaurada por copia comparada con `cmp`** —«RESTAURADO IGUAL» las tres—: **(N1)** quitar la siembra entera, que es el estado exacto de la etapa 3; **(N2)** sembrar las cuatro cuentas y **no afiliarlas** al grupo; **(N3)** sembrar tambien la de `identidad`, recorriendo `SistemasDelProducto.TODOS` en vez de `Consumidor.values()` | **3, 2 y 2 en rojo — y el defecto no hubo que provocarlo: estaba medido contra las cinco aplicaciones levantadas.** <br><br>**Lo que se midio, y por que no se veia desde aqui.** Con las cinco aplicaciones en pie (`scratchpad/ac56/informe.md`, hallazgo H5), `reconciliar-identidades.sh servicios` deja el cliente confidencial de cada satelite con su clave y su `municipalidad_id`, asi que **el consumidor consigue su token**; el buzon lo sirve este sistema y autoriza contra **su** tabla `usuario`, donde `service-account-kamayuk-<sistema>-servicio-<ubigeo>` **no existia**. Los cuatro recibian **403 `SIN_PRIVILEGIO`** «La cuenta «service-account-kamayuk-normativa-servicio-200105» no esta dada de alta en este sistema» y la pasada del consumidor de cada implantacion moria con `IdentidadNoContesta: identidad contesto 403 al leer el buzon`. Para poder medir AC-6 hubo que darlas de alta **a mano** con el token del administrador, por la API (`POST /seguridad/usuarios` y `POST /seguridad/grupos/2/miembros`, los cuatro). El grupo nacia sin miembros desde la etapa 3 y su javadoc lo decia; **lo que la medida anade es que nada lo llena**, asi que en un despliegue de verdad la copia local de los cuatro se queda como la dejo su implantacion **sin un solo error que lo diga** — el defecto exacto que la etapa 4 existe para cerrar. La etapa 3 razono que «a quien se afilia lo decide quien despliegue»; ejecutado, no lo decide nadie, y por eso esa decision se invierte aqui en vez de anotarse. <br><br>**(N1) da 3 en rojo y el primero es el 403 literal, con la cuenta dentro**: «[la implantacion tiene que dejar a «rentas» pudiendo leer el buzon. Si esto es 403, el consumidor de ese sistema muere en cada vuelta con «identidad contesto 403 al leer el buzon» y su copia local se queda como la dejo su implantacion, sin un solo error que lo diga …] contesto: `{"detail":"La cuenta «service-account-kamayuk-rentas-servicio-200105» no esta dada de alta en este sistema. No es que le falte un privilegio: no tiene ninguna ficha aqui …"}` expected: 200 but was: 403», mas las dos de las cuentas del grupo, «Expecting actual: [] to contain exactly … [las cuatro]». <br><br>**(N2) es la que separa las dos negativas, y da un 403 DISTINTO**: con las cuatro cuentas dadas de alta y sin afiliar, el mismo GET contesta «`{"detail":"No tiene el privilegio LECTURA sobre eventos","properties":{"codigo":"SIN_PRIVILEGIO"…}}`». Son los dos mensajes que `GuardiaDeAcceso` separa desde #29 §8 —uno se arregla dando un permiso y el otro no—, y aqui los dos son de la misma pieza: **dar de alta sin afiliar deja a los cuatro exactamente igual de parados**, con un mensaje que manda a mirar los permisos de una cuenta que no los va a tener nunca por si sola. Sin esta rotura, «los cuatro leen» se habria podido cumplir a medias. <br><br>**(N3) da 2 en rojo y los dos nombran la cuenta de mas**: «Expecting actual: […, "service-account-kamayuk-identidad-servicio-200105", …] to contain exactly … but some elements were not expected: ["service-account-kamayuk-identidad-servicio-200105"]», con el motivo dentro de la asercion —«este sistema no se consume a si mismo: lo que el buzon publica es lo que esta misma base acaba de escribir, asi que su cuenta no tendria nada que leer y su acuse retiraria un evento que nadie ha aplicado»—. Es tambien la medida de por que la lista se deriva de **`Consumidor`** y no de `SistemasDelProducto.TODOS` menos `identidad`: el enumerado es el mismo del que sale quien puede leer el buzon y el que `ConsumidorTest` ata al `CHECK` de `V3`, asi que una segunda lista se separaria — y la que se quedaria vieja seria justo la que da de alta las cuentas. <br><br>**La forma de la cuenta se compone donde ya se analizaba, y las dos direcciones se miden.** `Consumidor` gana `clienteDeServicio(ubigeo)` —`kamayuk-<sistema>-servicio-<ubigeo>`— y `cuentaDeServicio(ubigeo)` —el mismo con el prefijo `service-account-` que Keycloak le pone a la cuenta de servicio de un cliente confidencial, que es el `preferred_username` que llega en el token—. Componerla en la implantacion habria sido escribir esa forma dos veces, y **la que se quedaria vieja seria la que da de alta la cuenta**: su sintoma es una fila de `usuario` que ningun token nombra, o sea el mismo 403 con una fila mas en la tabla. `ConsumidorTest` compone y vuelve a analizar los cuatro, y fija el literal medido contra el emisor. Y un ubigeo que no son seis digitos **no compone**: lanza diciendolo, porque dar de alta una cuenta que ningun token puede nombrar sale en verde y no sirve para nada. <br><br>**Dos decisiones de dominio, con su motivo.** (1) **Se dan de alta habilitadas y con `Vigencia.SIEMPRE`**: una cuenta de servicio que naciera deshabilitada dejaria la replica parada desde el primer minuto con el mismo sintoma mudo que esto viene a cerrar, y una vigencia puesta por el despliegue caduca un dia que nadie recuerda y lo que se para entonces son **los cuatro sistemas a la vez** sin que nadie haya decidido nada — retirarle el acceso a un consumidor es un acto (desafiliarlo, o retirarle la credencial en el emisor), con su observacion y su auditoria, no un temporizador. Es lo mismo que ya valia para el administrador. (2) **Reimplantar es idempotente y se reafilia a proposito**: `CuentaRepetida` se atrapa y se lee la fila que hay —igual que con el administrador— y `afiliar` es un `ON CONFLICT … DO UPDATE` que **reactiva**, de modo que un despliegue **repara** a un consumidor al que alguien desafilio, que es la unica forma que tiene de repararse. Lo que cuesta es lo que ya costaba la afiliacion del administrador: el estado no crece y **los eventos si** —repetir la afiliacion es otro acto—, y aplicar dos veces un `MIEMBRO_AFILIADO` en la copia de un satelite es idempotente porque alli tambien es un `upsert`. Lo mide `segundoDespliegue()`: `usuario` sigue en **5** y las cuatro siguen dentro del grupo. <br><br>**El instrumento es el guardia de PRODUCCION, y eso obligo a una arista de pruebas que se dice.** `ElBuzonAutorizaALosConsumidoresTest` pone `GuardiaDeAcceso` —el interceptor que corre en la aplicacion— delante de `EventosController` con `ComprobadorDeAccesoJdbc` **de `seguridad`**, envuelto en el mismo `TransactionInterceptor` que los casos de uso porque sus consultas leen tablas con RLS y sin `SET LOCAL` **revientan** en vez de devolver vacio. Escribir un comprobador en la prueba habria medido esa copia: la precedencia usuario-sobre-grupo, el `a.sistema = 'identidad'` y la vigencia son suyos y son justo lo que decide si estas cuatro cuentas autorizan. Por eso `kamayuk-identidad-nucleo` gana `testImplementation(project(":kamayuk-identidad-seguridad"))`, **solo en pruebas**: `main` de ese modulo no ve `seguridad` y Spring Modulith sigue verificando lo mismo. Y el contraste que impide que «los cuatro leen» se cumpla con un guardia que diga que si a todo: la cuenta de servicio de **otra municipalidad** contesta 403 nombrandola. <br><br>**Cifras, medidas en el mismo entorno** (PostgreSQL 16.13 nativo en el 5432, JDK 25.0.4, `./gradlew build verificarArquitectura verificarAislamiento verificarArranque --continue --rerun-tasks --max-workers=2 --no-daemon`): **577 → 582 pruebas, 0 fallos, 0 omitidas**, `BUILD SUCCESSFUL in 3m 59s`. Las **+5** son 3 de `ElBuzonAutorizaALosConsumidoresTest` y 2 de `ConsumidorTest`; `nucleo` pasa de **70 a 75** y los otros cinco modulos no se mueven (aplicacion 116+4, dominio-compartido 154, esquema 43, plataforma 181, seguridad 9). **Y lo que la implantacion emite sube de 162 a 170 eventos** con el catalogo de hoy: 158 `PERMISO_FIJADO` (las 157 opciones del catalogo unido mas la del grupo del buzon), 2 `GRUPO_DADO_DE_ALTA`, **5** `USUARIO_DADO_DE_ALTA` y **5** `MIEMBRO_AFILIADO` —eran 1 y 1—. La cifra la afirma `ImplantacionEmiteSusEventosTest` **compuesta del tamano del catalogo** y no escrita a mano, para que estrenar una pantalla no la vuelva falsa; y las cuatro altas y las cuatro afiliaciones **viajan a los cuatro satelites**, que es correcto: cada uno autoriza contra su copia local, asi que una copia que las tiene y otra que no son distinguibles. <br><br>**Lo que NO cierra, con su motivo.** (a) **Que las cuatro cuentas existan en el EMISOR sigue siendo del despliegue** (`reconciliar-identidades.sh servicios`, `infrastructure`#21 AC-1): esto da de alta la fila de `usuario` con la que el guardia las conoce, no el cliente confidencial con el que piden el token. Las dos mitades hacen falta y viven en repositorios distintos; lo que ya no puede pasar es que una este y la otra no en silencio. (b) **No se ejercio contra las cinco aplicaciones levantadas otra vez**: lo medido aqui es de HTTP a PostgreSQL con MockMvc y el guardia real, y lo que motiva el cambio es la medicion de `scratchpad/ac56` con las cinco en pie. (c) **La segunda mitad del mensaje del guardia sigue siendo falsa** —«la administracion de usuarios, grupos y permisos vive en rentas»— y no se toca aqui: `GuardiaDeAcceso` es de `plataforma`, una de las piezas que `lo-que-los-cinco-comparten` compara byte a byte, y arreglarla en una copia suma una divergencia; se corrige en `kamayuk-lib` (ADR-0038). Queda dicho en el javadoc de `EventosController`. (d) **Los otros siete hallazgos de esa medicion no son de este repositorio** —H1 y H2 son de `rentas`, H3 del `.env.ejemplo`, H4 y H8 de `catastro`, H6 y H7 del consumidor de cada satelite— y siguen abiertos. |
| **La misma etapa 4, tercer commit ([#10](https://github.com/hneyra/identidad/pull/10)) — la regla 12 estaba DESACTIVADA justo en el unico repositorio donde tiene que estar encendida** (`ConfiguracionDeIdentidad.escritoresDeLaAutorizacionConMotivo()` declara `AdministracionRepositoryJdbc` y `PermisoRepositoryJdbc`, **sin fecha de fin**, con el motivo junto a cada entrada; el javadoc dice que enciende y que costaba; y la tabla de reglas de este `CLAUDE.md` gana su fila 12) | **Dos roturas, cada una aplicada SOLA y restaurada por copia comparada con `cmp`** —«RESTAURADO IGUAL» las dos—: **(R1)** quitar `PermisoRepositoryJdbc` de la lista; **(R2)** declarar `SembradorDeLaCopiaLocal`, que en este repositorio **no existe** —es la clase de los tres satelites—, que es la direccion de #27. Mas la **medicion previa** con la lista declarada **vacia**, que es lo que dice quienes son los escritores de verdad | **1 y 1 en rojo, y el defecto no hubo que provocarlo: estaba en `main` y se lee en la salida de la propia prueba.** <br><br>**El estado de partida.** `grep -rn "escritoresDeLaAutorizacionConMotivo"` sobre este repositorio daba **cero**, asi que el gancho devolvia `null` y la regla 12 **no vigilaba nada aqui** — en un verde que no era silencioso, porque `ProhibicionesEnElCodigoFuenteTest > esteRepositorioDiceSiVigilaLaAutorizacion()` lo imprimia en cada corrida: «[ADR-0039] la escritura de usuario/grupo/miembro/permiso NO se vigila en este repositorio: su configuracion no declara escritoresDeLaAutorizacionConMotivo(), asi que la prohibicion esta DESACTIVADA aqui. No es «no hay escrituras»: es «no se ha mirado». Remedio: declararla con sus escritores, su motivo y su fecha de fin». O sea: el interruptor hacia exactamente lo que se construyo para hacer y nadie habia atendido el aviso — y mientras tanto **dos cosas escritas afirmaban lo contrario**: la tabla del cuerpo de `infrastructure`#55 promete que `identidad` declara esos dos «sin fecha: es el dueño», y la fila del registro de la etapa 2 de `infrastructure` dice haberlo medido «con sus dos escritores declarados». En `main` no estaba. Con la lista puesta, la misma linea dice «SI se vigila aqui; escritores declarados: [AdministracionRepositoryJdbc…, PermisoRepositoryJdbc…]». <br><br>**Quienes son, medido antes de escribirlo.** Con la lista declarada **vacia**, el escaner señala **siete** escrituras en **dos** archivos y ni una mas: `PermisoRepositoryJdbc` lineas **407** (`INSERT INTO permiso`) y **440** (`UPDATE permiso SET`), y `AdministracionRepositoryJdbc` lineas **173** (`INSERT INTO grupo`), **193** (`UPDATE grupo`), **248** (`INSERT INTO usuario`), **276** (`UPDATE usuario`) y **374** (`INSERT INTO miembro`). **`ImplantarMunicipalidad` NO aparece**, y era lo que habia que comprobar: la etapa 4 le hizo sembrar las cuatro cuentas de servicio, pero escribe **por** `AdministrarSeguridad` y no con SQL propio — que es justo lo que la regla quiere, porque asi la escritura pasa por el caso de uso que exige observacion, asienta auditoria y publica el hecho en el buzon. Un tercer escritor con SQL propio tendria que entrar en la lista con su motivo, o dejar de escribir. <br><br>**(R1) da 1 en rojo nombrando archivo y linea, dos veces**: «`kamayuk-identidad-nucleo/src/main/java/kamayuk/identidad/nucleo/infraestructura/PermisoRepositoryJdbc.java` — ADR-0039: la autorizacion es un sistema y su dueño es «identidad». Esta clase ESCRIBE usuario/grupo/miembro/permiso, y dos sistemas que escriben la misma tabla de permisos en dos bases no dan un error: dan dos respuestas a «quien puede hacer esto» … **linea 407: INSERT INTO permiso**» y la de la **440**. <br><br>**(R2) es la direccion de #27 y da 1**: «escritoresDeLaAutorizacionConMotivo() → «SembradorDeLaCopiaLocal»: no hay ninguna clase de produccion con ese nombre, asi que no exime a nadie de ADR-0039 … una entrada que no nombra nada la hace decir de mas, y el dia que nazca una clase con ese nombre podra escribir la autorizacion sin que nadie lo haya decidido». <br><br>**Y una tercera cosa se midio sin querer, escribiendo mal la lista la primera vez.** Las entradas se probaron como «Clase: motivo», con el motivo dentro de la cadena; eso **no exime a nadie**: el escaner compara `eximidos.contains(claseDe(archivo))`, o sea el nombre simple contra el del archivo. El resultado fueron **2 rojas a la vez** —las siete escrituras seguian saliendo **y** #27 señalaba las dos entradas por no nombrar ninguna clase, con las dos frases enteras dentro del rojo—. El motivo pasa al comentario de cada entrada, que es la forma que las otras listas «ConMotivo» de los cinco repositorios ya usaban, y queda escrito en el javadoc para que nadie lo vuelva a intentar. <br><br>**Cifras**: **582 pruebas, 0 fallos, 0 omitidas**, `BUILD SUCCESSFUL in 1m 35s` con el comando de siempre. **La cuenta no se mueve, y es lo que tiene que pasar**: declarar no anade pruebas — enciende una que ya corria y que hasta ahora revisaba el conjunto vacio. <br><br>**LO QUE ESTO CUESTA, y hay que decirlo antes de mezclar**: `escritoresDeLaAutorizacionConMotivo()` **solo existe en la interfaz de `comun-verificaciones` a partir de [`infrastructure`#55](https://github.com/hneyra/infrastructure/pull/55)** (medido contra `infrastructure@bb3654b`, que es donde se compilo todo esto). Con `infrastructure@main`, el `@Override` no tiene nada que sobrescribir y **este repositorio deja de compilar**, exactamente igual que les pasa hoy a los otros cuatro. El rojo de su CI **cambia de forma**: hoy falla en `losCuatroPublicanSuContratoConIdentidad()` —esperando a que los cuatro consumidores publiquen su contrato— y pasara a fallar al compilar. Es el estado correcto mientras #55 no se mezcle, y **no se compra el verde quitando el `@Override`**: eso volveria a apagar la regla en el unico sitio donde tiene que estar encendida, que es de donde se sale. |
| **Etapa 5 de [`infrastructure`#52](https://github.com/hneyra/infrastructure/issues/52) ([#5](https://github.com/hneyra/identidad/issues/5)) — este repositorio NO cambia, y eso es lo que hay que anotar** (los cuatro satelites retiran `SembradorDeLaCopiaLocal`; `infrastructure` deriva el orden de implantacion de los manifiestos y escribe su runbook; aqui no se toca ni una linea de `src/main`) | **No hay rotura que aplicar, y decirlo es el punto.** Lo que la etapa 5 retira vive en los cuatro satelites: sus PR llevan sus roturas y sus rojos ([`rentas`#62](https://github.com/hneyra/rentas/pull/62), [`catastro`#93](https://github.com/hneyra/catastro/pull/93), [`normativa`#31](https://github.com/hneyra/normativa/pull/31), [`caja`#57](https://github.com/hneyra/caja/pull/57)); el orden y su guarda, el PR de `infrastructure` | **Lo que cambia aqui es lo que este repositorio SIGNIFICA, no su codigo.** Desde la etapa 5 este es **el unico sitio del producto donde se escribe la autorizacion**: en los otros cuatro, `usuario`, `grupo`, `miembro` y `permiso` los escribe **solo** su consumidor, con lo que este buzon publica. La regla 12 lo dice desde la etapa 4 en la tabla de arriba, y desde esta etapa **es cierta en los cinco a la vez**: los cuatro satelites declaran un unico escritor —su `AplicarUnEventoDeIdentidad` o su `CopiaLocalDeLaAutorizacionJdbc`—, sin fecha de fin, y su sembrador se queda en `SembradorDelCatalogo` con `modulo_sistema` y `acceso`.<br><br>**Y con eso el arranque en frio pasa a depender de este sistema, que es lo que la etapa 5 compra y lo que cuesta.** Una municipalidad implantada en un satelite **antes** que aqui se queda sin una sola cuenta: nadie puede entrar, ni siquiera quien acaba de implantarla. Los cuatro fallan diciendolo —con tres mensajes distintos, porque se arreglan de tres maneras: falta configuracion, `identidad` no contesta, o el orden esta al reves— en vez de terminar en verde con la copia vacia. El orden esta escrito en [`identidad-5-el-orden-de-implantacion.md`](https://github.com/hneyra/infrastructure/blob/main/docs/00-gobierno/identidad-5-el-orden-de-implantacion.md) y comprobado en `infra/verificaciones/orden-de-implantacion.test.ts`, **derivado de los manifiestos**: este sistema no nombra el espacio de nombres de ningun hermano y exactamente los cuatro nombran el suyo.<br><br>**Lo que NO cierra, con su motivo.** (a) **En un ambiente de cero los cuatro `Job` de implantacion aguantan unos 70 s reintentando** y este sistema tarda mas en migrar e implantar; es [`infrastructure`#65](https://github.com/hneyra/infrastructure/issues/65) y la decision es del dueño del despliegue. (b) **El grupo «Consumidores del buzon» y sus cuatro cuentas siguen naciendo aqui** (etapa 4) y eso no cambia: lo que la etapa 5 retira es el administrador de la copia de los satelites, no las cuentas de servicio. (c) **La segunda mitad del mensaje del guardia sigue siendo falsa** —«la administracion … vive en rentas»—: es de `plataforma`, pieza comparada byte a byte, y se corrige en `kamayuk-lib` (ADR-0038). |
| **El realm deja de llamarse `sgtm`, y este repositorio SE LEVANTA por primera vez** ([`infrastructure`#70](https://github.com/hneyra/infrastructure/issues/70)) (el realm `sgtm`→`kamayuk` y `sgtm-ciudadano`→`kamayuk-ciudadano` en los **seis** repositorios —52 archivos medidos, 21 que rompen—, con los clientes `kamayuk-{backoffice,verificacion,portal}`; dos guardas nuevas en `infrastructure` —`el-nombre-del-realm.test.ts`, que ata el realm del JSON al de los dos stacks, y `el-realm-cabe-en-keycloak.test.ts`, que mide los `varchar(255)`—; `restaurar-ambitos-de-fabrica.sh`; el `-q name=` de `reconciliar-identidades.sh` corregido; y **`despliegue/pruebas-e2e/`**: tres `.http` con las 23 operaciones y `ejercer.sh` con **55 casos** que afirman) | **Cinco roturas, cada una aplicada SOLA y restaurada por copia comparada con `cmp`** —«RESTAURADO IGUAL» las cinco—: **(R1)** que el stack de `stg` despliegue un realm distinto del que levanta el compose; **(R2)** que el realm del ciudadano deje de derivarse del de funcionarios; **(R3)** un `clientId` renombrado solo en el archivo versionado; **(R4)** el `description` del ambito de vuelta a sus 390 caracteres; **(R5)** la etiqueta de Traefik reclamando `PathPrefix(/identidad-otro)`, con el contenedor recreado | **1, 1, 1, 1 y exit 1 con 52 de 52 en rojo — y el trabajo empezo por un rojo que no hubo que provocar.**<br><br>**Lo primero que se midio: este repositorio nunca se habia levantado, y las cinco filas de arriba lo dicen** —«(d) el compose no se levanto ni la imagen se construyo: sin demonio de Docker en esta maquina»—. Esta maquina **si** lo tiene (Docker 29.1.3, Compose 2.40.3) y los otros cuatro sistemas ya se habian construido aqui; de `identidad` no habia **ni una imagen ni un contenedor**. Asi que por primera vez se ejercen su `compose.yaml` y su `Dockerfile`, y **cuatro afirmaciones de esta tabla caducan de golpe**.<br><br>**El defecto de la base no hubo que provocarlo: estaba.** Con el volumen del **2026-09-06 22:41** y el montaje de `identidad` entrando en `plataforma.compose.yaml` el **2026-09-09**, `psql -c '\\l'` daba `caja, catastro, normativa, rentas, postgres` y **siete `kamayuk_prueba_*` abandonadas** — **`identidad` no estaba**, que es exactamente lo que `05-crear-bases.sh:16-17` anticipa («su migrador muere con "database identidad does not exist"»). Los tres guiones de init se montan en `/docker-entrypoint-initdb.d/` y **solo corren con el directorio de datos vacio**, asi que un volumen preexistente **no gana la base de un sistema nuevo y nada lo avisa**. Recreado el volumen, las cinco bases y los tres roles del cluster.<br><br>**(R1) a (R3) miden la guarda que cierra el hueco de #70, y el hueco era real.** El nombre del realm se escribia en **dos** sitios sin que nada los comparara: el campo `realm` del JSON versionado (camino del compose) y `infra/config.ts:589` (camino del cluster, que `Identidad.ts:324` **impone** sobre el del archivo). Medido: cambiar los dos **no fue suficiente**, porque `Pulumi.stg.yaml:104` y `Pulumi.prod.yaml:119` fijan `kamayuk:keycloakRealm: sgtm` **explicitamente** y el valor por omision de `config.ts` no se alcanza en ningun ambiente real — **un tercer sitio que la exploracion no encontro y que solo salio al correr las pruebas**. Los rojos: «[«stg» despliega un realm distinto del que levanta el compose: el navegador entraria a un emisor y los backends validarian contra otro, y el 401 no dice por que] expected 'kamayuk-viejo' to be 'kamayuk'»; «[el segundo emisor no es `<realm>-ciudadano` … el portal validaria contra un realm que nadie importa]»; y «el cliente del backoffice no esta en el realm versionado: expected [ 'kamayuk-oficina', …(1) ] to include 'kamayuk-backoffice'».<br><br>**(R4) es un defecto que estaba en `main` y que impedia arrancar la plataforma entera** ([#71](https://github.com/hneyra/infrastructure/issues/71)). Keycloak quedaba en `Exited (1)`: el `description` del ambito `kamayuk-servicio` mide **390** caracteres y `CLIENT_SCOPE.DESCRIPTION` es `varchar(255)`. Comprobado con `git show main:` —**son 390 tambien en `main`**—, y no lo vio nadie porque `--import-realm` **solo importa la primera vez**: aparece cuando alguien recrea el contenedor. Con la guarda puesta, devolver el texto largo da «[Keycloak guarda estos campos en `varchar(255)`: una cadena mas larga no se recorta, hace fallar el `--import-realm` y **el contenedor no arranca** …] + "clientScope «kamayuk-servicio».description: 390 caracteres"».<br><br>**Y detras de ese salieron TRES defectos mas, los tres del mismo mecanismo —«`--import-realm` solo corre una vez», o sea invisibles hasta que alguien levanta el producto en limpio—.** (a) [#72](https://github.com/hneyra/infrastructure/issues/72): declarar `clientScopes` **no vacio** en el realm versionado **borra los trece ambitos de fabrica** de Keycloak; sin `profile` no hay `preferred_username` y sin `basic` no hay `sub`, que son los dos claims con los que `OrigenContextFilter` identifica la cuenta, asi que **todo funcionario recibe 403 con un token valido**. El contraste que lo confirma esta en el repositorio: `realm-sgtm-ciudadano.json` declara `clientScopes: []` **vacio** y **conserva los trece**. (b) el mismo #72 destapo que `reconciliar-identidades.sh` elegia el id del ambito con `-q name=kamayuk-servicio … | head -1`, y **ese endpoint de Keycloak ignora el parametro**: con catorce ambitos, `head -1` daba `basic`. Funcionaba por casualidad mientras el realm tuviera dos. (c) [#73](https://github.com/hneyra/infrastructure/issues/73), que es el mas serio: **el claim `municipalidad_id` se escribe con TRES valores distintos** —el **ubigeo** (`200105`) para las cuentas de servicio (`:458`), el `municipalidadId` del JSON (**9**) para los funcionarios (`:547,558,570`), y el id que la **secuencia** asigna al implantar (**1**)—, y **nada los reconcilia**. Su sintoma es el peor posible: **403 «La cuenta «administrador» no esta dada de alta en este sistema» con la fila delante en la tabla**, porque el `SET LOCAL` fija otro inquilino y el RLS esconde las cinco filas. Manda a mirar el alta, que es justo lo que esta bien.<br><br>**(R5) demuestra que el e2e muerde**: con la etiqueta de Traefik reclamando otro prefijo y el contenedor recreado, `ejercer.sh` sale con **codigo 1 y 52 de 52 casos en rojo**, el primero diciendo «esperaba 200 y contesto 404». Restaurado, vuelve a **55 casos, 0 fallos**. Y **un caso lo escribi mal y la medida me corrigio**: `DELETE /seguridad/usuarios/{id}` da **404 y no 405**, y el backend tiene razon —esa ruta no tiene ningun manejador, asi que no hay verbo admitido que anunciar—; el 405 se ejerce contra `/grupos/{id}/baja`, que existe y solo por POST, y el 404 se afirma aparte.<br><br>**Cifras, medidas en esta maquina** (Docker 29.1.3, PostgreSQL 16 por Testcontainers, JDK 25.0.4, `infrastructure@4272aca` de base): **las dos imagenes se construyeron por primera vez** (`kamayuk-identidad:compose` 583 MB, `kamayuk-identidad-migrador:compose` 505 MB, exit 0); la cadena levanto con los dos Job en `Exited (0)` y el backend **Healthy**; y **la prediccion de la tabla se cumplio exactamente**: **170 eventos** —158 `PERMISO_FIJADO`, 2 `GRUPO_DADO_DE_ALTA`, 5 `USUARIO_DADO_DE_ALTA`, 5 `MIEMBRO_AFILIADO`—, **5 cuentas** (`administrador` mas las cuatro `service-account-kamayuk-<sistema>-servicio-200105`, **sin** la de `identidad`), 2 grupos, 157 opciones y 17 modulos. `ejercer.sh`: **55 casos, 0 fallos**, de los que **16 son negativos**. `infrastructure`: **964 → 972** pruebas (+5 de `el-nombre-del-realm`, +3 de `el-realm-cabe-en-keycloak`). Los cinco descriptores en verde: `identidad` 21, `rentas` 29, `catastro` 19, `normativa` 17, `caja` 44. `rentas/frontend` **50 de 50** en las tres suites de identidad. La autoprueba del registro, «Las 11 muestras se comportan como deben, y las 4 rutas … tienen quien las ejerza».<br><br>**Lo que NO cierra, con su motivo.** (a) **Los cuatro rodeos manuales siguen siendo manuales**: restaurar los ambitos, la clave del administrador, los clientes de servicio y alinear el inquilino de las cinco cuentas. Estan escritos paso a paso en `despliegue/pruebas-e2e/README.md` **con el issue de cada uno al lado**, para que desaparezcan de ahi el dia que se cierren; ninguno se arregla desde este repositorio. (b) **Los `docs/D0-desarrollo/` de los cinco documentan dos comandos que no arrancan** ([#74](https://github.com/hneyra/infrastructure/issues/74)): el compose de los sistemas no ve el `.env` de la plataforma —hace falta `--env-file ../infrastructure/despliegue/.env`, que no esta escrito en ningun sitio— y los dos guiones de Keycloak no encuentran `plataforma.compose.yaml` sin `COMPOSE_FILE`. (c) **La suite de `infrastructure` tiene cinco pruebas que se caen por CARGA de CPU y no por defecto, y hubo que medirlo para no atribuirselas a este trabajo**: con el `build` de Gradle corriendo al lado salian rojas `api-que-no-contesta`, las dos de `puerto-del-motor`, `reglas-de-eslint` y `siembra-de-la-demostracion` —y **cada corrida senalaba unas distintas**, que es lo que lo delato—. Con la maquina libre: **972 de 972 y los 50 archivos en verde**. Las cuatro que se midieron sueltas tardan 4 421 ms (`api-que-no-contesta`, en `main` limpio), 2 168 ms (`reglas-de-eslint`), 512 ms y 554 ms — contra un tope de **5 000 ms**, asi que la primera vive al borde. (d) **No se ejercio ningun consumidor de verdad**: lo que se mide es el buzon con un token de servicio. (e) **La ventana de inconsistencia sigue sin medirse**: exige las cinco aplicaciones levantadas. (f) **Los dos archivos de realm SI se renombraron** a `realm-kamayuk{,-ciudadano}.json`, al final y contra lo que el issue estimaba: eran **31** referencias y no 7 —codigo, compose, CI, dos README y un ADR—, pero un archivo que se llama `sgtm` y dentro dice `kamayuk` engana al lector. Verificado recreando el contenedor de Keycloak: «Full importing from file …/realm-kamayuk.json». (g) **El guion de reparacion costo dos medidas por una trampa que conviene no redescubrir**: `kc()` es `docker compose exec -T`, que **consume stdin**, asi que dentro de un `while read … <<< "$lista"` se traga el resto y el bucle muere en la primera vuelta **sin error** — dejaba a los clientes con cero ambitos, en verde. Las listas pasan a `mapfile` y el guion gana un **centinela** que falla si un cliente se queda sin ninguno, que es lo que habria cazado esto a la primera. (h) **El `type` de los cuerpos de error sigue siendo `https://kamayuk.gob.pe/errores/<codigo>`**, y es el `sgtm` mas visible que queda —el frontend lo ve en cada 4xx—: queda senalado y fuera del alcance de este trabajo, que era el realm. |
| **`sgtm` sale del codigo de este repositorio, y la prosa se queda a proposito** ([`infrastructure`#76](https://github.com/hneyra/infrastructure/issues/76)) (el `type` de los cuerpos de error pasa a `https://kamayuk.gob.pe/errores/`; los identificadores internos —el prefijo de las bases de prueba, los tres parametros SQL con nombre **y sus enlaces**, el modulo de Jackson—; `SgtmAplicacion` → **`KamayukAplicacion`**, renombrada en los cinco a la vez; y `CLAUDE.md` gana la seccion que dice por que la prosa NO se toca — **582 → 582 pruebas**) | **No hay rotura propia que aplicar, y decirlo es el punto**: lo que vigila que la cadena no vuelva vive en `infrastructure` (`sin-el-nombre-del-monolito.test.ts`), y sus dos roturas estan en la fila de #76 de ese repositorio. Lo que si se midio aqui es el **rojo de (R1) contra ESTE arbol**: devolver `https://sgtm.gob.pe/errores/` a `ManejadorDeErrores.java:315` | **1 en rojo, y antes de eso un VERDE que era el defecto.**<br><br>**La primera vez que se probo, la guarda de `infrastructure` dejo pasar ese cambio EN VERDE**, y la causa importa aqui porque es codigo de este repositorio el que quedaba sin vigilar: su filtro de comentarios quitaba todo desde `//`, y **`https://` lleva `//`**. O sea que el `type` de los cuerpos de error —la superficie que el frontend recibe en **cada** 4xx— era invisible para la guarda que decia cubrirlo. Con el limite puesto, la misma rotura da 1 en rojo nombrando `backend/kamayuk-identidad-plataforma/…/ManejadorDeErrores.java:315`.<br><br>**`KamayukAplicacion` se renombro en los CINCO a la vez, y la razon esta en su propio javadoc.** Decia que el nombre «es el mismo en los cinco, y se conserva a proposito […] renombrarlo aqui haria que el quinto sistema fuera el unico distinto en lo unico que todos tienen igual». **La justificacion no era que se llamara `Sgtm`: era que los cinco se llamen igual**, asi que renombrarlos juntos conserva el invariante. El javadoc lo anota, y es el unico de los cinco que lo lleva porque es el que habla del quinto sistema.<br><br>**Cifras, medidas en esta maquina** (PostgreSQL 16 por Testcontainers, JDK 25.0.4, `./gradlew build verificarArquitectura`): **582 pruebas, 0 fallos, 0 omitidas**. La cuenta no se mueve, y es lo que tiene que pasar: esto renombra, no anade comportamiento. `spotlessApply` reformateo el modulo `-aplicacion` porque `KamayukAplicacion` es cuatro caracteres mas largo.<br><br>**Lo que NO se toca en este repositorio, con su motivo.** (a) **Las cabeceras de `V1__baseline.sql`** siguen diciendo «NO SE COPIA NINGUNA MIGRACION DE `sgtm`»: son comentarios, y explican una decision. (b) **Los comentarios del `Dockerfile`** sobre el jar heredado. (c) **Las filas de esta tabla** que citan el monolito: son el registro de lo que se midio entonces. La seccion nueva de arriba lo dice, para que nadie haga esa pasada por error. |
| **El e2e deja de necesitar cuatro pasos a mano, y sus puertos dejan de estar escritos** (`despliegue/pruebas-e2e/ejercer.sh` **deriva** `INGRESO`, `KEYCLOAK`, `UBIGEO` y `ADMINISTRADOR` del `.env` de la plataforma en vez de traerlos escritos; `despliegue/pruebas-e2e/README.md` **204 -> 150 lineas**, con sus 78 lineas de pasos manuales convertidas en un comando y los tres rodeos conservados en una tabla con su issue; y `docs/D0-desarrollo/README.md`, que mandaba el `up` **sin `--env-file`**) | **Las dos direcciones de la derivacion, ejecutadas**: con el `.env` de esta maquina delante, y con `ENV_DE_LA_PLATAFORMA` apuntando a un archivo que no existe. Y el e2e entero contra la pila levantada desde cero | **Los puertos escritos eran los de UNA maquina, y esa es la medida.** `ejercer.sh` traia `8082` y `8181` —los de aqui—; `.env.ejemplo` y los seis `docs/D0-desarrollo/` dicen **8080** y **8180**. Un CI que copiara el ejemplo al pie de la letra levantaria Keycloak donde el arnes no lo busca, y el sintoma seria «Connection refused» a un servicio que esta perfectamente arriba. Es `infrastructure`#74. <br><br>**Medido, las dos ramas**: con el `.env` presente imprime «Contra http://localhost:8082 (realm «kamayuk», ubigeo 200105, administrador «administrador»)» y «Puertos derivados de …/infrastructure/despliegue/.env»; con `ENV_DE_LA_PLATAFORMA=/no/existe`, «Contra http://localhost:8080 … ubigeo 200101, administrador «jperez»» y «SIN .env de la plataforma: puertos por omision, los de .env.ejemplo». O sea que el ultimo recurso son los valores que **D0 documenta**, y no los de la maquina donde esto se escribio. Y el `.env` se lee con `sed` de las cuatro lineas que hacen falta y no con `source`: un `source` del archivo entero meteria en este proceso las **seis claves del motor**, que este guion no necesita para nada. <br><br>**El README pasa de 78 lineas de pasos a un comando, y los rodeos NO se borran.** Se quedan en una tabla de cuatro filas —cada una con su issue y con el sintoma que produce su ausencia—, porque los defectos siguen abiertos: el dia que se cierren, desaparecen del guion **y de la tabla** a la vez. Y la fila 4 dice por que el orden no es de gusto: el paso que alinea `municipalidad_id` necesita el `id` que la SECUENCIA le dio a la municipalidad, y esa fila la escribe la implantacion de este sistema. <br><br>**Cifras, medidas con la pila levantada DESDE CERO** —`down -v` de los dos composes con el volumen borrado, y sin un solo paso manual—: `infrastructure/despliegue/levantar-todo.sh identidad` en **513 s**, y despues `ejercer.sh` en **55 casos, 0 fallos**, 6 s, con las dos credenciales que el guion imprimio. La base nueva se nota en lo que el e2e mide de paso: **5 cuentas** exactas —`administrador` y las cuatro `service-account-kamayuk-<sistema>-servicio-200105`— sin los `jperez-e2e-*` que dejaban las corridas anteriores, y **157** opciones en el catalogo unido. <br><br>**Y una rotura de las de `infrastructure` se ve DESDE AQUI, que es lo que hace util tener el arnes.** Con la clave del administrador fijada como TEMPORAL (`crear-usuario.sh --reset`), `ejercer.sh` sale con **codigo 2** y su primera linea es «no hubo token de persona: {"error":"invalid_grant","error_description":"Invalid user credentials"}» — un mensaje que se lee como una clave mal escrita y que en realidad dice «Keycloak exige cambiarla al entrar». Con la clave permanente, token de **1 403** caracteres y los 55 casos en verde. <br><br>**Lo que NO cambia, y es lo que hay que anotar**: ni una linea de `src/main`, ni del descriptor, ni del esquema. Lo que cambia es que levantar esto y comprobarlo **ya no depende de que alguien siga un README**. El flujo que lo ejerce vive en `infrastructure` —[`arranque-en-limpio.yml`](https://github.com/hneyra/infrastructure/blob/main/.github/workflows/arranque-en-limpio.yml)— y no aqui, a proposito: los tres defectos que motivan todo esto (#71, #72, #73) son del realm y de la plataforma, asi que **el rojo tiene que salir donde se causa y no donde se sufre**. |
